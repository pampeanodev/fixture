# i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Internacionalizar la app a ES/EN/PT con una capa i18n in-house, tipada, sin deps nuevas.

**Architecture:** React Context (`LocaleProvider`) expone `t(key, vars?)`, `locale`, `setLocale`, y helpers `formatDate/formatTime/formatNumber`. Bundles por locale en `src/i18n/locales/{es,en,pt}.ts`. ES es source of truth del tipo; EN/PT usan `satisfies Messages` para validación en compile-time. Tests de consistencia garantizan que no falte ninguna key ni ningún nombre de equipo.

**Tech Stack:** React 19, TypeScript 6, Vite 8, Vitest, `Intl.DateTimeFormat`, `Intl.PluralRules`.

**Spec:** [`docs/superpowers/specs/2026-04-21-i18n-design.md`](../specs/2026-04-21-i18n-design.md)

---

## Convenciones del plan

- Cada task es un commit atómico.
- Tests-first cuando aplica (módulos puros). Para componentes React, el test es "compila + `pnpm run build` + smoke manual" — seguimos el patrón del codebase (los `__tests__/` existentes cubren lógica pura, no componentes).
- Cada fase mantiene `pnpm test`, `pnpm lint`, y `pnpm run build` verdes.
- Locale codes: `"es" | "en" | "pt"`. BCP47 mapping: `es-AR`, `en-US`, `pt-BR`.

---

## Fase 1 — Infraestructura

Crea el paquete i18n entero con ES como source of truth. EN/PT son copias exactas de ES (placeholders). Ningún componente consume el hook todavía.

### Task 1: Crear tipo `LocaleCode` y constantes

**Files:**
- Create: `src/i18n/types.ts`

- [ ] **Step 1: Crear el archivo de tipos base**

```ts
// src/i18n/types.ts
export const SUPPORTED_LOCALES = ["es", "en", "pt"] as const;
export type LocaleCode = (typeof SUPPORTED_LOCALES)[number];

export const DEFAULT_LOCALE: LocaleCode = "es";

export const BCP47: Record<LocaleCode, string> = {
  es: "es-AR",
  en: "en-US",
  pt: "pt-BR",
};

export const NATIVE_NAMES: Record<LocaleCode, string> = {
  es: "Español",
  en: "English",
  pt: "Português",
};
```

- [ ] **Step 2: Commit**

```bash
git add src/i18n/types.ts
git commit -m "feat(i18n): add locale types and BCP47 mapping"
```

---

### Task 2: Bundle ES — source of truth

**Files:**
- Create: `src/i18n/locales/es.ts`

- [ ] **Step 1: Crear el bundle ES completo con todas las keys proyectadas**

El bundle ES incluye todas las secciones de la app. Los valores son los strings actuales hardcodeados en los componentes, consolidados.

```ts
// src/i18n/locales/es.ts
export const es = {
  common: {
    save: "Guardar",
    cancel: "Cancelar",
    close: "Cerrar",
    copy: "Copiar",
    copied: "Copiado",
    back: "Volver",
    confirm: "Confirmar",
    error: "Error",
    loading: "Cargando...",
    retry: "Reintentar",
  },

  meta: {
    title: "Mundial 2026 — Prode",
    description: "Fixture interactivo del Mundial 2026 con predicciones y sync P2P vía Nostr.",
  },

  brand: {
    short: "Mundial 2026",
  },

  sidebar: {
    nav: {
      schedule: "Calendario",
      groups: "Grupos",
      knockout: "Eliminatorias",
      ranking: "Ranking",
      rooms: "Salas",
      simulation: "Simulación",
    },
    footer: {
      myAccount: "Mi cuenta",
      donate: "Donar",
      github: "GitHub",
      license: "GPL-3.0",
      copy: "© 2026 Pampeano Dev",
      githubAria: "Repositorio en GitHub",
    },
  },

  topbar: {
    namePlaceholder: "Tu nombre",
    mode: {
      results: "Resultados",
      predictions: "Predicciones",
    },
    menu: {
      sectionPredictions: "Predicciones",
      sectionSimulation: "Simulación",
      sectionResults: "Resultados",
      sectionFixture: "Fixture",
      randomize: "Regenerar random",
      startSimulation: "Iniciar simulación",
      exitSimulation: "Salir de simulación",
      fetchApi: "Fetch desde API (próximamente)",
      fetchApiTitle: "Integración con API externa: próximamente",
      exportAll: "Exportar todo",
      importAll: "Importar todo",
    },
    importError: "Error al importar. Verificá que sea un JSON válido.",
  },

  groups: {
    title: "Grupo {group}",
    standings: {
      pos: "Pos",
      team: "Equipo",
      played: "PJ",
      won: "G",
      drawn: "E",
      lost: "P",
      goalsFor: "GF",
      goalsAgainst: "GC",
      goalDifference: "DG",
      points: "Pts",
    },
    legend: {
      qualified: "Clasifica directo",
      thirdPlace: "Puede pasar como mejor tercero",
    },
    matches: "Partidos",
  },

  knockout: {
    rounds: {
      R32: "32avos",
      R16: "Octavos",
      QF: "Cuartos",
      SF: "Semis",
      "3P": "3er puesto",
      F: "Final",
    },
    slot: {
      groupWinner: "1° {group}",
      groupRunnerUp: "2° {group}",
      bestThird: "3° ({groups})",
      winnerOf: "Ganador {matchId}",
      loserOf: "Perdedor {matchId}",
    },
  },

  schedule: {
    title: "Calendario",
    empty: "Sin partidos para mostrar.",
    stage: {
      group: "Grupo {group}",
      R32: "32avos",
      R16: "Octavos",
      QF: "Cuartos",
      SF: "Semis",
      "3P": "3er puesto",
      F: "Final",
    },
  },

  ranking: {
    title: "Ranking",
    empty: "Todavía no hay puntos cargados.",
    you: "Vos",
    headerName: "Jugador",
    headerPoints: "Pts",
    pointsOne: "{count} punto",
    pointsOther: "{count} puntos",
    rivalAdd: "Agregar rival manual",
    rivalRemove: "Quitar",
  },

  scoreInput: {
    ariaHome: "Goles {team}",
    ariaAway: "Goles {team}",
    lockedTitle: "Cerrado — falta 1 h para el kickoff",
  },

  rooms: {
    list: {
      title: "Salas",
      empty: "No estás en ninguna sala todavía.",
      createCta: "Crear sala",
      joinCta: "Unirme con código",
      joinPlaceholder: "Pegá el código de 8 caracteres",
      joinButton: "Unirme",
      invalidCode: "Código inválido.",
    },
    create: {
      title: "Crear sala",
      nameLabel: "Nombre",
      namePlaceholder: "Ej: Amigos del asado",
      typeOpen: "Abierta",
      typeClosed: "Cerrada (invitación)",
      submit: "Crear",
      openHelp: "Cualquiera con el link entra.",
      closedHelp: "Generás invites de un solo uso.",
    },
    detail: {
      membersTitle: "Miembros",
      adminBadge: "Admin",
      leaveButton: "Salir de la sala",
      leaveConfirm: "¿Salir de esta sala?",
      inviteButton: "Invitar",
      lockedTitle: "Sala bloqueada",
      lockedBody: "Las predicciones quedaron cerradas.",
    },
    invite: {
      title: "Invitar a la sala",
      linkLabel: "Link compartible",
      codeLabel: "Código de 8 caracteres",
      qrLabel: "Escaneá el QR",
      copyLink: "Copiar link",
      copyCode: "Copiar código",
    },
    connection: {
      connected: "Conectado",
      connecting: "Conectando...",
      disconnected: "Sin conexión",
    },
  },

  onboarding: {
    title: "Mundial 2026",
    subtitle: "Competí con tus amigos prediciendo resultados.",
    namePlaceholder: "Tu nombre",
    createButton: "Empezar",
    restoreCta: "Ya tengo cuenta",
    restoreTextarea: "Escribí tus 12 palabras separadas por espacios",
    restoreButton: "Restaurar",
    backButton: "Volver",
    invalidMnemonic: "Seed phrase inválida. Verificá las 12 palabras.",
  },

  account: {
    title: "Mi cuenta",
    description: "Tu identidad es una seed phrase de 12 palabras (NIP-06).",
    showSeed: "Ver mi seed phrase",
    warning: "No la compartas con nadie. Quien tenga estas palabras controla tu identidad.",
    qrTitle: "QR de backup",
    copySeed: "Copiar seed",
    copied: "Copiado",
  },

  donate: {
    title: "Donar con Lightning",
    intro: "Si el proyecto te resultó útil, invitame un café con sats:",
    addressLabel: "Dirección Lightning",
    copied: "Copiado",
    close: "Cerrar",
  },

  simulator: {
    title: "Simulador",
    stageLabel: {
      group: "Grupo {group}",
      R32: "32avos",
      R16: "Octavos",
      QF: "Cuartos",
      SF: "Semis",
      "3P": "3er puesto",
      F: "Final",
    },
    randomButton: "Random",
    manualButton: "Manual",
    skipButton: "Saltar",
    exitButton: "Salir de simulación",
    winner: "{team} gana",
    draw: "Empate",
    penaltiesWinner: "{team} gana por penales",
    previousMatch: "Partido anterior",
    rankingTitle: "Ranking en vivo",
    pointsDelta: "+{delta}",
    noMoreMatches: "No hay más partidos para simular.",
  },

  tour: {
    overview: {
      welcomeTitle: "Bienvenido al Prode 2026",
      welcomeBody: "Predecís los 104 partidos del Mundial y ganás puntos cuando acertás. <1>3 pts</1> resultado exacto · <2>1 pt</2> solo ganador · <3>0</3> errado.",
      modeTitle: "Resultados vs Predicciones",
      modeBody: "Cambiás entre cargar lo que pensás que va a pasar (Predicciones) y lo que pasó realmente (Resultados). Tus puntos salen de comparar ambos.",
      groupsTitle: "Grupos",
      groupsBody: "72 partidos de fase de grupos divididos en 12 grupos (A a L). La tabla se recalcula sola con tus predicciones.",
      knockoutTitle: "Eliminatorias",
      knockoutBody: "32 partidos desde 32avos hasta la final. Los cruces se arman automáticamente según tus predicciones de grupos — no los definís a mano.",
      roomsTitle: "Salas",
      roomsBody: "Competí con amigos. Creás una sala, compartís el link, y los rankings se sincronizan peer-to-peer (sin backend).",
      helpTitle: "¿Dudas más adelante?",
      helpBody: "Tocá este botón en cualquier pantalla para abrir un tour específico de lo que estás viendo.",
    },
    groups: {
      tabsTitle: "Grupos A–L",
      tabsBody: "Cambiás entre los 12 grupos desde estos tabs.",
      standingsTitle: "Tabla de posiciones",
      standingsBody: "Se recalcula en vivo con tus predicciones. Verde = clasifica directo a 32avos. Amarillo = 3er puesto, puede pasar como mejor tercero junto a otros 7 de otros grupos.",
      matchesTitle: "Cargar predicción",
      matchesBody: "Escribí el score de cada partido. Se guarda solo. Si estás en modo Resultados, cargás el resultado real. Desde el menú ⋯ podés regenerar todas las predicciones random de un toque.",
    },
    knockout: {
      tabsTitle: "Rondas de eliminatorias",
      tabsBody: "Navegás entre 32avos, Octavos, Cuartos, Semis y Final desde estos tabs.",
      autoTitle: "Cruces automáticos",
      autoBody: "Los equipos que aparecen en cada cruce salen de tus predicciones de grupos (1° de A, 2° de B, mejores terceros, etc.). Cuando cargás un partido, el ganador pasa solo al siguiente cruce.",
    },
    rooms: {
      createTitle: "Crear sala",
      createBody: "Ponele nombre y elegí el tipo: abierta (cualquiera con el link entra) o cerrada (generás invites de un solo uso). Te da un código de 8 caracteres y un link para compartir.",
      joinTitle: "Unirme con código",
      joinBody: "Si alguien te pasó un código de 8 caracteres, pegalo acá. Lo más cómodo igual es abrir el link de invitación que te manden — te mete solo.",
      commitTitle: "Commit-reveal",
      commitBody: "Tus predicciones se publican encriptadas hasta 1 hora antes del kickoff de cada partido. Nadie las puede ver antes (ni el relay ni los rivales).",
    },
    simulator: {
      modeTitle: "Modo simulación",
      modeBody: "Jugá el mundial entero antes que empiece. Para cada partido podés simular random, cargar un score manual, o saltarlo.",
      rankingTitle: "Ranking en vivo",
      rankingBody: "Después de cada partido ves el ranking actualizado con flechas (↑↓) según quién subió o bajó.",
      safeTitle: "No afecta tu prode real",
      safeBody: "Cuando salís de simulación, todo vuelve al estado real. Tus predicciones quedan intactas. Si recargás el browser, también volvés al real.",
    },
  },

  teams: {
    MEX: "México", RSA: "Sudáfrica", KOR: "Corea del Sur", CZE: "Chequia",
    CAN: "Canadá", SUI: "Suiza", QAT: "Qatar", BIH: "Bosnia y Herzegovina",
    BRA: "Brasil", MAR: "Marruecos", HAI: "Haití", SCO: "Escocia",
    USA: "Estados Unidos", PAR: "Paraguay", AUS: "Australia", TUR: "Turquía",
    GER: "Alemania", CUW: "Curazao", CIV: "Costa de Marfil", ECU: "Ecuador",
    NED: "Países Bajos", JPN: "Japón", TUN: "Túnez", SWE: "Suecia",
    BEL: "Bélgica", EGY: "Egipto", IRN: "Irán", NZL: "Nueva Zelanda",
    ESP: "España", CPV: "Cabo Verde", KSA: "Arabia Saudita", URU: "Uruguay",
    FRA: "Francia", SEN: "Senegal", NOR: "Noruega", IRQ: "Irak",
    ARG: "Argentina", ALG: "Argelia", AUT: "Austria", JOR: "Jordania",
    POR: "Portugal", UZB: "Uzbekistán", COL: "Colombia", COD: "RD Congo",
    ENG: "Inglaterra", CRO: "Croacia", GHA: "Ghana", PAN: "Panamá",
  },
} as const;

export type Messages = typeof es;
```

- [ ] **Step 2: Commit**

```bash
git add src/i18n/locales/es.ts
git commit -m "feat(i18n): add ES locale bundle as source of truth"
```

---

### Task 3: Bundles EN y PT — copias exactas de ES (placeholder)

**Files:**
- Create: `src/i18n/locales/en.ts`
- Create: `src/i18n/locales/pt.ts`

- [ ] **Step 1: Crear `en.ts` apuntando al objeto ES**

```ts
// src/i18n/locales/en.ts
// Fase 4 reemplaza este alias por traducciones reales con `satisfies Messages`.
import type { Messages } from "./es";
import { es } from "./es";

export const en = es as unknown as Messages;
```

- [ ] **Step 2: Crear `pt.ts` idéntico**

```ts
// src/i18n/locales/pt.ts
// Fase 4 reemplaza este alias por traducciones reales con `satisfies Messages`.
import type { Messages } from "./es";
import { es } from "./es";

export const pt = es as unknown as Messages;
```

- [ ] **Step 3: Commit**

```bash
git add src/i18n/locales/en.ts src/i18n/locales/pt.ts
git commit -m "feat(i18n): add EN/PT bundles as ES aliases (placeholder)"
```

---

### Task 4: Tipo `MessageKey` con `Path<T>` recursivo

**Files:**
- Create: `src/i18n/keys.ts`
- Test: `src/i18n/__tests__/keys.test-d.ts`

- [ ] **Step 1: Crear el tipo recursivo**

```ts
// src/i18n/keys.ts
import type { Messages } from "./locales/es";

type Path<T, Prefix extends string = ""> = {
  [K in keyof T & string]: T[K] extends string
    ? `${Prefix}${K}`
    : T[K] extends object
      ? Path<T[K], `${Prefix}${K}.`>
      : never;
}[keyof T & string];

export type MessageKey = Path<Messages>;

// Para campos con variantes pluralizadas (termina en "One" / "Other"),
// la "base" es la key sin sufijo. Pluralization helper acepta la base.
export type PluralBaseKey = {
  [K in MessageKey]: K extends `${infer Base}One` ? Base : never;
}[MessageKey];

// Variables de interpolación: cualquier record string -> string|number.
export type Vars = Record<string, string | number>;
```

- [ ] **Step 2: Crear smoke test de tipos**

```ts
// src/i18n/__tests__/keys.test-d.ts
import type { MessageKey, PluralBaseKey } from "../keys";

const ok1: MessageKey = "topbar.mode.predictions";
const ok2: MessageKey = "groups.title";
const ok3: MessageKey = "teams.ARG";
const okPlural: PluralBaseKey = "ranking.points";

// @ts-expect-error — key inexistente
const bad1: MessageKey = "topbar.mode.unknown";
// @ts-expect-error — path que apunta a un objeto, no a string
const bad2: MessageKey = "topbar.mode";

export { ok1, ok2, ok3, okPlural, bad1, bad2 };
```

- [ ] **Step 3: Validar que compila**

Run: `pnpm tsc -b`
Expected: build succeeds sin errores en `keys.test-d.ts`.

- [ ] **Step 4: Commit**

```bash
git add src/i18n/keys.ts src/i18n/__tests__/keys.test-d.ts
git commit -m "feat(i18n): add MessageKey recursive type"
```

---

### Task 5: `detectLocale` + test

**Files:**
- Create: `src/i18n/detect.ts`
- Create: `src/i18n/__tests__/detect.test.ts`

- [ ] **Step 1: Escribir el test primero**

```ts
// src/i18n/__tests__/detect.test.ts
import { describe, it, expect, vi, afterEach } from "vitest";
import { detectLocale } from "../detect";

function mockLanguages(langs: string[]) {
  Object.defineProperty(navigator, "languages", {
    value: langs,
    configurable: true,
  });
}

describe("detectLocale", () => {
  afterEach(() => {
    vi.restoreAllMocks();
  });

  it("returns 'es' when browser prefers Spanish", () => {
    mockLanguages(["es-AR", "en"]);
    expect(detectLocale()).toBe("es");
  });

  it("returns 'en' when browser prefers English", () => {
    mockLanguages(["en-US"]);
    expect(detectLocale()).toBe("en");
  });

  it("returns 'pt' for pt-BR", () => {
    mockLanguages(["pt-BR"]);
    expect(detectLocale()).toBe("pt");
  });

  it("returns 'pt' for pt-PT", () => {
    mockLanguages(["pt-PT"]);
    expect(detectLocale()).toBe("pt");
  });

  it("picks the first supported language in the preference order", () => {
    mockLanguages(["fr-FR", "en-GB", "es-AR"]);
    expect(detectLocale()).toBe("en");
  });

  it("falls back to 'es' when no language is supported", () => {
    mockLanguages(["ja-JP", "ko-KR"]);
    expect(detectLocale()).toBe("es");
  });

  it("handles empty navigator.languages", () => {
    mockLanguages([]);
    expect(detectLocale()).toBe("es");
  });
});
```

- [ ] **Step 2: Correr el test, verificar que falla**

Run: `pnpm vitest run src/i18n/__tests__/detect.test.ts`
Expected: FAIL con "Cannot find module '../detect'".

- [ ] **Step 3: Implementar `detect.ts`**

```ts
// src/i18n/detect.ts
import { SUPPORTED_LOCALES, DEFAULT_LOCALE, type LocaleCode } from "./types";

export function detectLocale(): LocaleCode {
  const langs = navigator.languages?.length
    ? navigator.languages
    : [navigator.language];

  for (const lang of langs) {
    const primary = lang.toLowerCase().split("-")[0];
    if ((SUPPORTED_LOCALES as readonly string[]).includes(primary)) {
      return primary as LocaleCode;
    }
  }
  return DEFAULT_LOCALE;
}
```

- [ ] **Step 4: Correr el test, verificar que pasa**

Run: `pnpm vitest run src/i18n/__tests__/detect.test.ts`
Expected: PASS (7 tests).

- [ ] **Step 5: Commit**

```bash
git add src/i18n/detect.ts src/i18n/__tests__/detect.test.ts
git commit -m "feat(i18n): add detectLocale with navigator.languages fallback"
```

---

### Task 6: Helpers de formato + test

**Files:**
- Create: `src/i18n/format.ts`
- Create: `src/i18n/__tests__/format.test.ts`

- [ ] **Step 1: Escribir el test primero**

```ts
// src/i18n/__tests__/format.test.ts
import { describe, it, expect } from "vitest";
import { formatMatchDate, formatNumber } from "../format";

const FIXED_UTC = "2026-06-11T20:00:00Z";

describe("formatMatchDate", () => {
  it("formats in es-AR with 24h time", () => {
    const result = formatMatchDate(FIXED_UTC, "es");
    expect(result).toMatch(/·/);
    expect(result).not.toMatch(/AM|PM|a\.\s?m\.|p\.\s?m\./i);
  });

  it("formats in en-US with 12h time", () => {
    const result = formatMatchDate(FIXED_UTC, "en");
    expect(result).toMatch(/·/);
    expect(result).toMatch(/AM|PM/i);
  });

  it("formats in pt-BR with 24h time", () => {
    const result = formatMatchDate(FIXED_UTC, "pt");
    expect(result).toMatch(/·/);
    expect(result).not.toMatch(/AM|PM/i);
  });

  it("produces different output per locale", () => {
    const es = formatMatchDate(FIXED_UTC, "es");
    const en = formatMatchDate(FIXED_UTC, "en");
    const pt = formatMatchDate(FIXED_UTC, "pt");
    expect(es).not.toBe(en);
    expect(en).not.toBe(pt);
  });
});

describe("formatNumber", () => {
  it("formats integers per locale", () => {
    expect(formatNumber(1234, "en")).toBe("1,234");
    expect(formatNumber(1234, "es")).toBe("1.234");
    expect(formatNumber(1234, "pt")).toBe("1.234");
  });
});
```

- [ ] **Step 2: Correr el test, verificar que falla**

Run: `pnpm vitest run src/i18n/__tests__/format.test.ts`
Expected: FAIL con "Cannot find module '../format'".

- [ ] **Step 3: Implementar `format.ts`**

```ts
// src/i18n/format.ts
import { BCP47, type LocaleCode } from "./types";

export function formatMatchDate(utcDate: string, locale: LocaleCode): string {
  const date = new Date(utcDate);
  const datePart = new Intl.DateTimeFormat(BCP47[locale], {
    weekday: "short",
    day: "numeric",
    month: "short",
  }).format(date);
  const timePart = new Intl.DateTimeFormat(BCP47[locale], {
    hour: "2-digit",
    minute: "2-digit",
    hour12: locale === "en",
  }).format(date);
  return `${datePart} · ${timePart}`;
}

export function formatTime(utcDate: string, locale: LocaleCode): string {
  const date = new Date(utcDate);
  return new Intl.DateTimeFormat(BCP47[locale], {
    hour: "2-digit",
    minute: "2-digit",
    hour12: locale === "en",
  }).format(date);
}

export function formatNumber(n: number, locale: LocaleCode): string {
  return new Intl.NumberFormat(BCP47[locale]).format(n);
}
```

- [ ] **Step 4: Correr el test, verificar que pasa**

Run: `pnpm vitest run src/i18n/__tests__/format.test.ts`
Expected: PASS (5 tests).

- [ ] **Step 5: Commit**

```bash
git add src/i18n/format.ts src/i18n/__tests__/format.test.ts
git commit -m "feat(i18n): add Intl-based formatMatchDate/formatTime/formatNumber"
```

---

### Task 7: Tests de consistencia de locales

**Files:**
- Create: `src/i18n/__tests__/locales.consistency.test.ts`

- [ ] **Step 1: Escribir el test**

```ts
// src/i18n/__tests__/locales.consistency.test.ts
import { describe, it, expect } from "vitest";
import { es } from "../locales/es";
import { en } from "../locales/en";
import { pt } from "../locales/pt";

type Tree = { [K: string]: Tree | string };

function collectKeys(obj: Tree, prefix = ""): string[] {
  const out: string[] = [];
  for (const [k, v] of Object.entries(obj)) {
    const path = prefix ? `${prefix}.${k}` : k;
    if (typeof v === "string") {
      out.push(path);
    } else if (v && typeof v === "object") {
      out.push(...collectKeys(v as Tree, path));
    }
  }
  return out.sort();
}

describe("locale bundle consistency", () => {
  const esKeys = collectKeys(es as unknown as Tree);

  it("en has the same keys as es", () => {
    const enKeys = collectKeys(en as unknown as Tree);
    const missing = esKeys.filter((k) => !enKeys.includes(k));
    const extra = enKeys.filter((k) => !esKeys.includes(k));
    expect(missing, `Missing keys in en: ${missing.join(", ")}`).toEqual([]);
    expect(extra, `Extra keys in en: ${extra.join(", ")}`).toEqual([]);
  });

  it("pt has the same keys as es", () => {
    const ptKeys = collectKeys(pt as unknown as Tree);
    const missing = esKeys.filter((k) => !ptKeys.includes(k));
    const extra = ptKeys.filter((k) => !esKeys.includes(k));
    expect(missing, `Missing keys in pt: ${missing.join(", ")}`).toEqual([]);
    expect(extra, `Extra keys in pt: ${extra.join(", ")}`).toEqual([]);
  });

  it("every plural key xxxOne has a matching xxxOther", () => {
    const oneKeys = esKeys.filter((k) => k.endsWith("One"));
    const missingOther = oneKeys.filter((k) => {
      const otherKey = k.slice(0, -3) + "Other";
      return !esKeys.includes(otherKey);
    });
    expect(missingOther, `One-keys without Other: ${missingOther.join(", ")}`).toEqual([]);
  });
});
```

- [ ] **Step 2: Correr el test, verificar que pasa** (porque EN/PT son aliases de ES)

Run: `pnpm vitest run src/i18n/__tests__/locales.consistency.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 3: Commit**

```bash
git add src/i18n/__tests__/locales.consistency.test.ts
git commit -m "test(i18n): add locale bundle consistency checks"
```

---

### Task 8: Test de consistencia de equipos

**Files:**
- Create: `src/i18n/__tests__/teams.consistency.test.ts`

- [ ] **Step 1: Escribir el test**

```ts
// src/i18n/__tests__/teams.consistency.test.ts
import { describe, it, expect } from "vitest";
import { TEAMS } from "../../data/teams";
import { es } from "../locales/es";
import { en } from "../locales/en";
import { pt } from "../locales/pt";

describe("team name consistency", () => {
  for (const team of TEAMS) {
    it(`${team.id} has a name in all locales`, () => {
      expect(es.teams, `es.teams.${team.id}`).toHaveProperty(team.id);
      expect(en.teams, `en.teams.${team.id}`).toHaveProperty(team.id);
      expect(pt.teams, `pt.teams.${team.id}`).toHaveProperty(team.id);
    });
  }

  it("no extra team entries beyond TEAMS", () => {
    const validIds = new Set(TEAMS.map((t) => t.id));
    for (const locale of [es, en, pt]) {
      const extras = Object.keys(locale.teams).filter((k) => !validIds.has(k));
      expect(extras, `Unknown team entries: ${extras.join(", ")}`).toEqual([]);
    }
  });
});
```

- [ ] **Step 2: Correr el test, verificar que pasa**

Run: `pnpm vitest run src/i18n/__tests__/teams.consistency.test.ts`
Expected: PASS (49 tests — 48 equipos + 1 "no extras").

- [ ] **Step 3: Commit**

```bash
git add src/i18n/__tests__/teams.consistency.test.ts
git commit -m "test(i18n): add team name consistency across locales"
```

---

### Task 9: Motor de `t()` con interpolación + pluralización

**Files:**
- Create: `src/i18n/translate.ts`
- Create: `src/i18n/__tests__/translate.test.ts`

- [ ] **Step 1: Escribir el test**

```ts
// src/i18n/__tests__/translate.test.ts
import { describe, it, expect } from "vitest";
import { createT } from "../translate";
import { es } from "../locales/es";

describe("createT", () => {
  const t = createT(es, "es");

  it("returns a simple string by path", () => {
    expect(t("topbar.mode.predictions")).toBe("Predicciones");
  });

  it("interpolates variables", () => {
    expect(t("groups.title", { group: "A" })).toBe("Grupo A");
  });

  it("handles plural base with count=1 → One", () => {
    expect(t("ranking.points", { count: 1 })).toBe("1 punto");
  });

  it("handles plural base with count=5 → Other", () => {
    expect(t("ranking.points", { count: 5 })).toBe("5 puntos");
  });

  it("interpolates count itself into plural variants", () => {
    expect(t("ranking.points", { count: 0 })).toBe("0 puntos");
  });

  it("returns the key itself if path is missing (dev-visible fallback)", () => {
    // @ts-expect-error — forzar path inválido para verificar fallback
    expect(t("nonexistent.path")).toBe("nonexistent.path");
  });

  it("works for dynamic team keys", () => {
    expect(t("teams.ARG")).toBe("Argentina");
  });
});
```

- [ ] **Step 2: Correr el test, verificar que falla**

Run: `pnpm vitest run src/i18n/__tests__/translate.test.ts`
Expected: FAIL con "Cannot find module '../translate'".

- [ ] **Step 3: Implementar `translate.ts`**

```ts
// src/i18n/translate.ts
import type { Messages } from "./locales/es";
import type { MessageKey, Vars } from "./keys";
import { BCP47, type LocaleCode } from "./types";

type Dict = { [k: string]: Dict | string };

function lookup(dict: Dict, path: string): string | undefined {
  const parts = path.split(".");
  let cur: Dict | string = dict;
  for (const p of parts) {
    if (typeof cur === "string") return undefined;
    if (cur[p] === undefined) return undefined;
    cur = cur[p];
  }
  return typeof cur === "string" ? cur : undefined;
}

function interpolate(template: string, vars: Vars): string {
  return template.replace(/\{(\w+)\}/g, (_, name) =>
    name in vars ? String(vars[name]) : `{${name}}`,
  );
}

export function createT(bundle: Messages, locale: LocaleCode) {
  const dict = bundle as unknown as Dict;
  const pluralRules = new Intl.PluralRules(BCP47[locale]);

  function t(key: MessageKey, vars?: Vars): string {
    // Plural: si hay `count`, intentar primero `${key}One` / `${key}Other`.
    if (vars && "count" in vars) {
      const rule = pluralRules.select(vars.count as number);
      const suffix = rule === "one" ? "One" : "Other";
      const pluralValue = lookup(dict, `${key}${suffix}`);
      if (pluralValue !== undefined) {
        return interpolate(pluralValue, vars);
      }
    }
    const value = lookup(dict, key);
    if (value === undefined) return key;
    return vars ? interpolate(value, vars) : value;
  }

  return t;
}

export type TFunction = ReturnType<typeof createT>;
```

- [ ] **Step 4: Correr el test**

Run: `pnpm vitest run src/i18n/__tests__/translate.test.ts`
Expected: PASS (7 tests).

- [ ] **Step 5: Commit**

```bash
git add src/i18n/translate.ts src/i18n/__tests__/translate.test.ts
git commit -m "feat(i18n): add t() with interpolation and Intl.PluralRules"
```

---

### Task 10: `LocaleProvider` + hook `useLocale`

**Files:**
- Create: `src/i18n/LocaleContext.tsx`

- [ ] **Step 1: Implementar el Provider y el hook**

```tsx
// src/i18n/LocaleContext.tsx
import { createContext, useCallback, useContext, useEffect, useMemo, useState } from "react";
import { DEFAULT_LOCALE, SUPPORTED_LOCALES, type LocaleCode } from "./types";
import { detectLocale } from "./detect";
import { formatMatchDate, formatNumber, formatTime } from "./format";
import { createT, type TFunction } from "./translate";
import { es } from "./locales/es";
import { en } from "./locales/en";
import { pt } from "./locales/pt";

const BUNDLES = { es, en, pt } as const;
const STORAGE_KEY = "fixture.locale";

function readStoredLocale(): LocaleCode | null {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (raw && (SUPPORTED_LOCALES as readonly string[]).includes(raw)) {
      return raw as LocaleCode;
    }
  } catch {
    // localStorage no disponible (private mode, SSR, etc.)
  }
  return null;
}

interface LocaleContextValue {
  locale: LocaleCode;
  setLocale: (next: LocaleCode) => void;
  t: TFunction;
  formatDate: (utcDate: string) => string;
  formatTime: (utcDate: string) => string;
  formatNumber: (n: number) => string;
}

const LocaleContext = createContext<LocaleContextValue | null>(null);

export function LocaleProvider({ children }: { children: React.ReactNode }) {
  const [locale, setLocaleState] = useState<LocaleCode>(
    () => readStoredLocale() ?? detectLocale() ?? DEFAULT_LOCALE,
  );

  const setLocale = useCallback((next: LocaleCode) => {
    try {
      localStorage.setItem(STORAGE_KEY, next);
    } catch {
      // ignore
    }
    setLocaleState(next);
  }, []);

  const value = useMemo<LocaleContextValue>(() => {
    const bundle = BUNDLES[locale];
    const t = createT(bundle, locale);
    return {
      locale,
      setLocale,
      t,
      formatDate: (utc) => formatMatchDate(utc, locale),
      formatTime: (utc) => formatTime(utc, locale),
      formatNumber: (n) => formatNumber(n, locale),
    };
  }, [locale, setLocale]);

  // Sincroniza <html lang> con el locale activo (accesibilidad).
  // Title y meta description se activan en Fase 5.
  useEffect(() => {
    document.documentElement.lang = locale;
  }, [locale]);

  return <LocaleContext.Provider value={value}>{children}</LocaleContext.Provider>;
}

export function useLocale(): LocaleContextValue {
  const ctx = useContext(LocaleContext);
  if (!ctx) throw new Error("useLocale must be used within LocaleProvider");
  return ctx;
}
```

- [ ] **Step 2: Verificar que compila**

Run: `pnpm tsc -b`
Expected: OK.

- [ ] **Step 3: Commit**

```bash
git add src/i18n/LocaleContext.tsx
git commit -m "feat(i18n): add LocaleProvider with localStorage persistence"
```

---

### Task 11: Componente `<Trans>` para rich text

**Files:**
- Create: `src/i18n/Trans.tsx`
- Create: `src/i18n/__tests__/Trans.test.tsx`

- [ ] **Step 1: Escribir el test**

```tsx
// src/i18n/__tests__/Trans.test.tsx
import { describe, it, expect } from "vitest";
import { renderToString } from "react-dom/server";
import { parseTransTemplate, Trans } from "../Trans";

describe("parseTransTemplate", () => {
  it("returns a single text segment when no placeholders", () => {
    expect(parseTransTemplate("Hello world")).toEqual([
      { kind: "text", value: "Hello world" },
    ]);
  });

  it("parses a single <1>...</1> placeholder", () => {
    expect(parseTransTemplate("a <1>bold</1> b")).toEqual([
      { kind: "text", value: "a " },
      { kind: "slot", id: 1, content: "bold" },
      { kind: "text", value: " b" },
    ]);
  });

  it("parses multiple placeholders", () => {
    expect(parseTransTemplate("<1>one</1> and <2>two</2>")).toEqual([
      { kind: "slot", id: 1, content: "one" },
      { kind: "text", value: " and " },
      { kind: "slot", id: 2, content: "two" },
    ]);
  });

  it("leaves malformed tags as plain text", () => {
    const out = parseTransTemplate("a <1>x");
    const flat = out
      .map((s) => (s.kind === "text" ? s.value : `<${s.id}>${s.content}</${s.id}>`))
      .join("");
    expect(flat).toBe("a <1>x");
  });
});

describe("<Trans>", () => {
  it("renders wrappers around the slot content", () => {
    const html = renderToString(
      <Trans template="a <1>b</1> c" components={{ 1: <strong /> }} />,
    );
    expect(html).toContain("<strong");
    expect(html).toContain("b");
  });
});
```

- [ ] **Step 2: Correr, verificar que falla**

Run: `pnpm vitest run src/i18n/__tests__/Trans.test.tsx`
Expected: FAIL.

- [ ] **Step 3: Implementar `Trans.tsx`**

Usamos `String.prototype.matchAll` para evitar el patrón stateful de regex.

```tsx
// src/i18n/Trans.tsx
import { cloneElement, Fragment, type ReactElement } from "react";

type Segment =
  | { kind: "text"; value: string }
  | { kind: "slot"; id: number; content: string };

const PATTERN = /<(\d+)>([\s\S]*?)<\/\1>/g;

// Export para tests. La regex tolera ids multi-dígito.
export function parseTransTemplate(template: string): Segment[] {
  const out: Segment[] = [];
  let cursor = 0;

  for (const match of template.matchAll(PATTERN)) {
    const matchIndex = match.index ?? 0;
    if (matchIndex > cursor) {
      out.push({ kind: "text", value: template.slice(cursor, matchIndex) });
    }
    out.push({ kind: "slot", id: Number(match[1]), content: match[2] });
    cursor = matchIndex + match[0].length;
  }

  if (cursor < template.length) {
    out.push({ kind: "text", value: template.slice(cursor) });
  }
  return out;
}

interface TransProps {
  template: string;
  components: Record<number, ReactElement>;
}

export function Trans({ template, components }: TransProps) {
  const segments = parseTransTemplate(template);
  return (
    <>
      {segments.map((seg, i) => {
        if (seg.kind === "text") return <Fragment key={i}>{seg.value}</Fragment>;
        const wrapper = components[seg.id];
        if (!wrapper) return <Fragment key={i}>{seg.content}</Fragment>;
        return cloneElement(wrapper, { key: i }, seg.content);
      })}
    </>
  );
}
```

- [ ] **Step 4: Correr el test**

Run: `pnpm vitest run src/i18n/__tests__/Trans.test.tsx`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/i18n/Trans.tsx src/i18n/__tests__/Trans.test.tsx
git commit -m "feat(i18n): add <Trans> component for tour rich text"
```

---

### Task 12: Barrel `index.ts`

**Files:**
- Create: `src/i18n/index.ts`

- [ ] **Step 1: Re-exports**

```ts
// src/i18n/index.ts
export { LocaleProvider, useLocale } from "./LocaleContext";
export { Trans } from "./Trans";
export { SUPPORTED_LOCALES, NATIVE_NAMES, type LocaleCode } from "./types";
export type { MessageKey } from "./keys";
```

- [ ] **Step 2: Commit**

```bash
git add src/i18n/index.ts
git commit -m "feat(i18n): add barrel index"
```

---

### Task 13: Wrappear `<App />` en `<LocaleProvider>` + validación de build

**Files:**
- Modify: `src/main.tsx`

- [ ] **Step 1: Actualizar `main.tsx`**

```tsx
// src/main.tsx
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import { LocaleProvider } from "./i18n";
import { FixtureProvider } from "./context/FixtureContext";
import { NostrProvider } from "./context/NostrContext";
import App from "./App";

createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <LocaleProvider>
      <NostrProvider>
        <FixtureProvider>
          <App />
        </FixtureProvider>
      </NostrProvider>
    </LocaleProvider>
  </StrictMode>,
);
```

- [ ] **Step 2: Correr toda la suite**

Run: `pnpm test && pnpm lint && pnpm run build`
Expected: todos verdes. La app sigue viéndose igual (nadie consume `useLocale` aún).

- [ ] **Step 3: Commit**

```bash
git add src/main.tsx
git commit -m "feat(i18n): mount LocaleProvider at app root"
```

---

**Fin de Fase 1.** La infraestructura está completa. Los tests de consistencia, detect, format, translate y Trans pasan. La app sigue renderizando en español exactamente igual que antes.

---

## Fase 2 — Migrar componentes y tour

Cada componente se migra en un commit separado. Si `team.name` aparece, **déjalo tal cual** — la Fase 3 se encarga de él (es un refactor del data model separado).

### Task 14: Migrar `Sidebar`

**Files:**
- Modify: `src/components/Sidebar.tsx`

- [ ] **Step 1: Reemplazar strings por `t()`**

```tsx
// src/components/Sidebar.tsx
import { useFixture } from "../context/FixtureContext";
import { useLocale } from "../i18n";
import { SidebarFooter } from "./SidebarFooter";
import "./Sidebar.css";

interface SidebarProps {
  collapsed: boolean;
  isMobile: boolean;
  onNavigate: () => void;
}

export function Sidebar({ collapsed, isMobile, onNavigate }: SidebarProps) {
  const { state, dispatch } = useFixture();
  const { t } = useLocale();
  const { activeView } = state;

  function navigate(view: typeof activeView) {
    dispatch({ type: "SET_VIEW", view });
    onNavigate();
  }

  const className = [
    "sidebar",
    collapsed ? "collapsed" : "",
    isMobile && !collapsed ? "open" : "",
  ].filter(Boolean).join(" ");

  return (
    <nav className={className}>
      <div className="sidebar-brand">
        <img src="/favicon.svg" alt="" className="sidebar-brand-logo" aria-hidden="true" />
        <span>{t("brand.short")}</span>
      </div>
      <div className="sidebar-nav">
        <div
          className={`sidebar-item ${activeView.type === "schedule" ? "active" : ""}`}
          onClick={() => navigate({ type: "schedule" })}>
          {t("sidebar.nav.schedule")}
        </div>
        <div
          data-tour="nav-groups"
          className={`sidebar-item ${activeView.type === "groups" ? "active" : ""}`}
          onClick={() => navigate({ type: "groups", group: activeView.type === "groups" ? activeView.group : "A" })}>
          {t("sidebar.nav.groups")}
        </div>
        <div
          data-tour="nav-knockout"
          className={`sidebar-item ${activeView.type === "knockout" ? "active" : ""}`}
          onClick={() => navigate({ type: "knockout", round: activeView.type === "knockout" ? activeView.round : "R32" })}>
          {t("sidebar.nav.knockout")}
        </div>
        <div className="sidebar-divider" />
        <div
          className={`sidebar-item ${activeView.type === "ranking" ? "active" : ""}`}
          onClick={() => navigate({ type: "ranking" })}>
          {t("sidebar.nav.ranking")}
          {state.rivals.length > 0 && (
            <span style={{ fontSize: "10px", opacity: 0.6, marginLeft: "6px" }}>
              {state.rivals.length + 1}
            </span>
          )}
        </div>
        <div
          data-tour="nav-rooms"
          className={`sidebar-item ${activeView.type === "rooms" || activeView.type === "room" ? "active" : ""}`}
          onClick={() => navigate({ type: "rooms" })}>
          {t("sidebar.nav.rooms")}
        </div>
        {state.simulationActive && (
          <div
            className={`sidebar-item ${activeView.type === "simulator" ? "active" : ""}`}
            onClick={() => navigate({ type: "simulator" })}>
            <span className="sim-dot">●</span> {t("sidebar.nav.simulation")}
          </div>
        )}
      </div>
      <SidebarFooter />
    </nav>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/Sidebar.tsx
git commit -m "refactor(i18n): migrate Sidebar to useLocale"
```

---

### Task 15: Migrar `SidebarFooter`

**Files:**
- Modify: `src/components/SidebarFooter.tsx`

- [ ] **Step 1: Reemplazar strings por `t()`**

En el cuerpo del componente, agregar `const { t } = useLocale();` e importar desde `"../i18n"`. Reemplazar:

- `"Mi cuenta"` → `{t("sidebar.footer.myAccount")}`
- `"Donar"` → `{t("sidebar.footer.donate")}`
- `aria-label="Repositorio en GitHub"` → `aria-label={t("sidebar.footer.githubAria")}`
- `"GitHub"` → `{t("sidebar.footer.github")}`
- `"GPL-3.0"` → `{t("sidebar.footer.license")}`
- `"© 2026 Pampeano Dev"` → `{t("sidebar.footer.copy")}`

- [ ] **Step 2: Commit**

```bash
git add src/components/SidebarFooter.tsx
git commit -m "refactor(i18n): migrate SidebarFooter to useLocale"
```

---

### Task 16: Migrar `TopBar`

**Files:**
- Modify: `src/components/TopBar.tsx`

- [ ] **Step 1: Reemplazar strings**

Agregar `const { t } = useLocale();` tras `useFixture()`. Reemplazar:

- `placeholder="Tu nombre"` → `placeholder={t("topbar.namePlaceholder")}`
- `"Resultados"` → `{t("topbar.mode.results")}`
- `"Predicciones"` (botón mode) → `{t("topbar.mode.predictions")}`
- `"Error al importar..."` (en el `alert`) → `alert(t("topbar.importError"))`
- En el dropdown:
  - `"Predicciones"` (section label) → `{t("topbar.menu.sectionPredictions")}`
  - `"Regenerar random"` → `{t("topbar.menu.randomize")}`
  - `"Simulación"` (section) → `{t("topbar.menu.sectionSimulation")}`
  - `"Iniciar simulación"` → `{t("topbar.menu.startSimulation")}`
  - `"Salir de simulación"` → `{t("topbar.menu.exitSimulation")}`
  - `"Resultados"` (section) → `{t("topbar.menu.sectionResults")}`
  - `title="Integración..."` → `title={t("topbar.menu.fetchApiTitle")}`
  - `"Fetch desde API..."` → `{t("topbar.menu.fetchApi")}`
  - `"Fixture"` → `{t("topbar.menu.sectionFixture")}`
  - `"Exportar todo"` → `{t("topbar.menu.exportAll")}`
  - `"Importar todo"` → `{t("topbar.menu.importAll")}`

- [ ] **Step 2: Commit**

```bash
git add src/components/TopBar.tsx
git commit -m "refactor(i18n): migrate TopBar to useLocale"
```

---

### Task 17: Migrar `Onboarding`

**Files:**
- Modify: `src/components/Onboarding.tsx`

- [ ] **Step 1: Reemplazar strings**

Importar `useLocale` y reemplazar cada literal:

- `<h1>Mundial 2026</h1>` → `<h1>{t("onboarding.title")}</h1>`
- `<p>Competi con tus amigos...</p>` → `{t("onboarding.subtitle")}`
- `placeholder="Tu nombre"` → `placeholder={t("onboarding.namePlaceholder")}`
- `"Empezar"` → `{t("onboarding.createButton")}`
- `"Ya tengo cuenta"` → `{t("onboarding.restoreCta")}`
- `placeholder="Escribi tus 12 palabras..."` → `placeholder={t("onboarding.restoreTextarea")}`
- `"Restaurar"` → `{t("onboarding.restoreButton")}`
- `"Volver"` → `{t("onboarding.backButton")}`
- `setError("Seed phrase invalida...")` → `setError(t("onboarding.invalidMnemonic"))`

- [ ] **Step 2: Commit**

```bash
git add src/components/Onboarding.tsx
git commit -m "refactor(i18n): migrate Onboarding to useLocale"
```

---

### Task 18: Migrar `GroupView`

**Files:**
- Modify: `src/components/GroupView.tsx`

- [ ] **Step 1: Leer el archivo**

Run: `cat src/components/GroupView.tsx` para ubicar cada string.

- [ ] **Step 2: Reemplazar strings**

Strings típicos a mapear:

- Título del grupo `Grupo X` → `t("groups.title", { group })`
- Headers de la tabla (Pos, Equipo, PJ, G, E, P, GF, GC, DG, Pts) → keys de `t("groups.standings.*")`
- Leyendas (Clasifica directo, Puede pasar como mejor tercero) → `t("groups.legend.*")`
- Sección "Partidos" → `t("groups.matches")`

No tocar `{team?.name}` todavía (Fase 3).

- [ ] **Step 3: Commit**

```bash
git add src/components/GroupView.tsx
git commit -m "refactor(i18n): migrate GroupView to useLocale"
```

---

### Task 19: Migrar `BracketView`

**Files:**
- Modify: `src/components/BracketView.tsx`

- [ ] **Step 1: Leer y mapear**

- Labels de ronda ("32avos", "Octavos", etc.) → `t(\`knockout.rounds.${round}\`)`. Atención: el tipo `KnockoutRound` incluye `"R32" | "R16" | "QF" | "SF" | "3P" | "F"`, las keys coinciden.
- Slot labels (`slotLabel()` function) — si devuelve strings como `"1° A"` o `"Ganador X"`, reescribirla para usar `t("knockout.slot.groupWinner", { group })`, `t("knockout.slot.bestThird", { groups })`, etc.

No tocar `homeTeam.name` (Fase 3).

- [ ] **Step 2: Commit**

```bash
git add src/components/BracketView.tsx
git commit -m "refactor(i18n): migrate BracketView to useLocale"
```

---

### Task 20: Migrar `ScheduleView` + usar `formatDate` del context

**Files:**
- Modify: `src/components/ScheduleView.tsx`

- [ ] **Step 1: Obtener `t` y `formatDate` del hook**

```tsx
const { t, formatDate, formatTime } = useLocale();
```

- [ ] **Step 2: Reemplazar el formateo manual**

- Sustituir `date.toLocaleDateString("es-AR", { weekday, day, month })` por `formatDate(match.dateUtc)` (el helper ya produce "Lun 11 Jun · 20:00").
- Si hay un `toLocaleTimeString("en-US", ...)` para el `time` separado, sustituir por `formatTime(match.dateUtc)`.

- [ ] **Step 3: Reemplazar strings**

- Título "Calendario" → `t("schedule.title")`
- Labels de stage (`Grupo A`, `32avos`, etc.) → `t("schedule.stage.group", { group })` o `t(\`schedule.stage.${round}\`)` según el tipo de partido.
- Empty state → `t("schedule.empty")`

No tocar `team.name` (Fase 3).

- [ ] **Step 4: Commit**

```bash
git add src/components/ScheduleView.tsx
git commit -m "refactor(i18n): migrate ScheduleView to useLocale and shared formatDate"
```

---

### Task 21: Migrar `RankingView` con pluralización

**Files:**
- Modify: `src/components/RankingView.tsx`

- [ ] **Step 1: Reemplazar strings**

- Título "Ranking" → `t("ranking.title")`
- Empty state → `t("ranking.empty")`
- Headers ("Jugador", "Pts") → `t("ranking.headerName")`, `t("ranking.headerPoints")`
- "Vos" (badge del player actual) → `t("ranking.you")`
- Cualquier `${n} puntos` → `t("ranking.points", { count: n })`
- Botones de rival manual → `t("ranking.rivalAdd")` / `t("ranking.rivalRemove")`

- [ ] **Step 2: Commit**

```bash
git add src/components/RankingView.tsx
git commit -m "refactor(i18n): migrate RankingView with pluralized points"
```

---

### Task 22: Migrar `ScoreInput`

**Files:**
- Modify: `src/components/ScoreInput.tsx`

- [ ] **Step 1: Reemplazar strings**

- `aria-label` de goles → `t("scoreInput.ariaHome", { team })` / `t("scoreInput.ariaAway", { team })`.
  - Para el parámetro `team`, usar el ID (`team.id`) por ahora; Fase 3 lo migrará a `t(\`teams.${id}\`)` pasado como string ya resuelto.
- `title` del lock → `t("scoreInput.lockedTitle")`

- [ ] **Step 2: Commit**

```bash
git add src/components/ScoreInput.tsx
git commit -m "refactor(i18n): migrate ScoreInput to useLocale"
```

---

### Task 23: Migrar `RoomList`

**Files:**
- Modify: `src/components/RoomList.tsx`

- [ ] **Step 1: Reemplazar strings**

- Título → `t("rooms.list.title")`
- Empty → `t("rooms.list.empty")`
- CTAs ("Crear sala", "Unirme con código") → `t("rooms.list.createCta")` / `t("rooms.list.joinCta")`
- Placeholder de código → `t("rooms.list.joinPlaceholder")`
- Botón "Unirme" → `t("rooms.list.joinButton")`
- Error "Código inválido" → `t("rooms.list.invalidCode")`
- Modal de creación: todas las keys bajo `t("rooms.create.*")`

- [ ] **Step 2: Commit**

```bash
git add src/components/RoomList.tsx
git commit -m "refactor(i18n): migrate RoomList to useLocale"
```

---

### Task 24: Migrar `RoomDetail`

**Files:**
- Modify: `src/components/RoomDetail.tsx`

- [ ] **Step 1: Reemplazar strings**

Usar keys bajo `t("rooms.detail.*")`:
- "Miembros" → `membersTitle`
- "Admin" badge → `adminBadge`
- "Salir de la sala" → `leaveButton`
- `confirm("¿Salir...?")` → `confirm(t("rooms.detail.leaveConfirm"))`
- "Invitar" → `inviteButton`
- Título/body del lock → `lockedTitle` / `lockedBody`

- [ ] **Step 2: Commit**

```bash
git add src/components/RoomDetail.tsx
git commit -m "refactor(i18n): migrate RoomDetail to useLocale"
```

---

### Task 25: Migrar `InviteModal` y `ConnectionStatus`

**Files:**
- Modify: `src/components/InviteModal.tsx`
- Modify: `src/components/ConnectionStatus.tsx`

- [ ] **Step 1: `InviteModal`**

Keys bajo `t("rooms.invite.*")`:
- Título → `title`
- Labels ("Link compartible", "Código de 8 caracteres", "Escaneá el QR") → `linkLabel`, `codeLabel`, `qrLabel`
- Botones → `copyLink`, `copyCode`

- [ ] **Step 2: `ConnectionStatus`**

Keys bajo `t("rooms.connection.*")`:
- `"Conectado"` → `connected`
- `"Conectando..."` → `connecting`
- `"Sin conexión"` → `disconnected`

- [ ] **Step 3: Commit**

```bash
git add src/components/InviteModal.tsx src/components/ConnectionStatus.tsx
git commit -m "refactor(i18n): migrate InviteModal and ConnectionStatus"
```

---

### Task 26: Migrar `AccountModal`

**Files:**
- Modify: `src/components/AccountModal.tsx`

- [ ] **Step 1: Reemplazar strings** usando keys bajo `t("account.*")` y `t("common.close")`, `t("common.copy")`, `t("common.copied")`.

Claves a mapear:
- Título → `account.title`
- Descripción → `account.description`
- Botón "Ver mi seed phrase" → `account.showSeed`
- Warning → `account.warning`
- "QR de backup" → `account.qrTitle`
- "Copiar" / "Copiado" → `common.copy` / `common.copied`
- Botón de cerrar (`&times;`) — no toca texto, el glifo queda.

- [ ] **Step 2: Commit**

```bash
git add src/components/AccountModal.tsx
git commit -m "refactor(i18n): migrate AccountModal to useLocale"
```

---

### Task 27: Migrar `DonateModal`

**Files:**
- Modify: `src/components/DonateModal.tsx`

- [ ] **Step 1: Keys bajo `t("donate.*")`**

- Título → `donate.title`
- Intro → `donate.intro`
- Label dirección → `donate.addressLabel`
- "Copiado" → `donate.copied` (o `common.copied`)
- "Cerrar" → `donate.close` (o `common.close`)

- [ ] **Step 2: Commit**

```bash
git add src/components/DonateModal.tsx
git commit -m "refactor(i18n): migrate DonateModal to useLocale"
```

---

### Task 28: Migrar `SimulatorView`

**Files:**
- Modify: `src/components/SimulatorView.tsx`

- [ ] **Step 1: Usar `useLocale()`**

Reemplazar strings y el `toLocaleString()` crudo:

- Título del simulator → `t("simulator.title")`
- `stageLabel` — el componente tiene lógica para construir "Grupo X" o nombre de ronda. Reescribirla para que devuelva:
  - Para partidos de grupo: `t("simulator.stageLabel.group", { group })`
  - Para knockout: `t(\`simulator.stageLabel.${round}\`)`
- Botones "Random", "Manual", "Saltar", "Salir de simulación" → `t("simulator.randomButton")` etc.
- "No hay más partidos para simular" → `t("simulator.noMoreMatches")`
- `new Date(match.dateUtc).toLocaleString()` → `formatDate(match.dateUtc)` (del hook).
- Función `getWinnerText` — mover a inline dentro del componente para que pueda llamar `t()`:
  - Empate → `t("simulator.draw")`
  - Ganador normal → `t("simulator.winner", { team: teamName })`
  - Ganador por penales → `t("simulator.penaltiesWinner", { team: teamName })`
- "Partido anterior" → `t("simulator.previousMatch")`
- "Ranking en vivo" → `t("simulator.rankingTitle")`
- Delta de puntos (`+3`, etc.) — ya es un número, no requiere i18n.

No tocar `team.name` todavía; se resuelve en Fase 3.

- [ ] **Step 2: Commit**

```bash
git add src/components/SimulatorView.tsx
git commit -m "refactor(i18n): migrate SimulatorView to useLocale"
```

---

### Task 29: Migrar `HelpButton`, `QRDisplay`

**Files:**
- Modify: `src/components/HelpButton.tsx`
- Modify: `src/components/QRDisplay.tsx`

- [ ] **Step 1: Revisar strings**

- `HelpButton` — el botón usa un glifo "?" sin texto, probablemente no tenga strings; verificar y si hay `title` o `aria-label`, agregar una key `common.help` o similar (ya tenemos `topbar.menu.*` pero no aplica; si aparece algo nuevo, agregar a `es.ts` y replicar en en/pt).
- `QRDisplay` — probablemente solo renderiza un `<canvas>`, sin texto. Verificar.

Si no hay strings, el task se completa sin cambios (en ese caso, no hace falta commit — marcar como completado).

- [ ] **Step 2: Commit (si hubo cambios)**

```bash
git add src/components/HelpButton.tsx src/components/QRDisplay.tsx
git commit -m "refactor(i18n): migrate HelpButton and QRDisplay"
```

---

### Task 30: Migrar `tour/steps.ts` a keys + HTML wrapper para rich text

**Files:**
- Modify: `src/tour/steps.ts`
- Modify: `src/tour/useTour.ts`

- [ ] **Step 1: Cambiar la firma de `TOURS` a una función que recibe `t`**

```ts
// src/tour/steps.ts
import type { DriveStep } from "driver.js";
import type { TFunction } from "../i18n/translate";

export type TourId = "overview" | "groups" | "knockout" | "rooms" | "simulator";

export function buildTours(t: TFunction): Record<TourId, DriveStep[]> {
  return {
    overview: [
      {
        popover: {
          title: t("tour.overview.welcomeTitle"),
          // driver.js acepta HTML en description; convertimos <N>...</N> a <strong>.
          description: renderTourHtml(t("tour.overview.welcomeBody")),
        },
      },
      {
        element: '[data-tour="mode-toggle"]',
        popover: {
          title: t("tour.overview.modeTitle"),
          description: t("tour.overview.modeBody"),
        },
      },
      {
        element: '[data-tour="nav-groups"]',
        popover: {
          title: t("tour.overview.groupsTitle"),
          description: t("tour.overview.groupsBody"),
        },
      },
      {
        element: '[data-tour="nav-knockout"]',
        popover: {
          title: t("tour.overview.knockoutTitle"),
          description: t("tour.overview.knockoutBody"),
        },
      },
      {
        element: '[data-tour="nav-rooms"]',
        popover: {
          title: t("tour.overview.roomsTitle"),
          description: t("tour.overview.roomsBody"),
        },
      },
      {
        element: '[data-tour="help-button"]',
        popover: {
          title: t("tour.overview.helpTitle"),
          description: t("tour.overview.helpBody"),
          side: "left",
        },
      },
    ],
    groups: [
      {
        element: '[data-tour="group-tabs"]',
        popover: { title: t("tour.groups.tabsTitle"), description: t("tour.groups.tabsBody") },
      },
      {
        element: '[data-tour="standings-table"]',
        popover: { title: t("tour.groups.standingsTitle"), description: t("tour.groups.standingsBody") },
      },
      {
        element: '[data-tour="match-cards"]',
        popover: { title: t("tour.groups.matchesTitle"), description: t("tour.groups.matchesBody") },
      },
    ],
    knockout: [
      {
        element: '[data-tour="round-tabs"]',
        popover: { title: t("tour.knockout.tabsTitle"), description: t("tour.knockout.tabsBody") },
      },
      {
        popover: { title: t("tour.knockout.autoTitle"), description: t("tour.knockout.autoBody") },
      },
    ],
    rooms: [
      {
        element: '[data-tour="room-create"]',
        popover: { title: t("tour.rooms.createTitle"), description: t("tour.rooms.createBody") },
      },
      {
        element: '[data-tour="room-join"]',
        popover: { title: t("tour.rooms.joinTitle"), description: t("tour.rooms.joinBody") },
      },
      {
        popover: { title: t("tour.rooms.commitTitle"), description: t("tour.rooms.commitBody") },
      },
    ],
    simulator: [
      {
        popover: { title: t("tour.simulator.modeTitle"), description: t("tour.simulator.modeBody") },
      },
      {
        popover: { title: t("tour.simulator.rankingTitle"), description: t("tour.simulator.rankingBody") },
      },
      {
        popover: { title: t("tour.simulator.safeTitle"), description: t("tour.simulator.safeBody") },
      },
    ],
  };
}

// Convierte placeholders <N>...</N> a <strong>...</strong> para driver.js.
// Los wrappers son siempre <strong>; el contenido entre tags se escapa.
function renderTourHtml(template: string): string {
  return template.replace(
    /<(\d+)>([\s\S]*?)<\/\1>/g,
    (_, _id, content) => `<strong>${escapeHtml(content)}</strong>`,
  );
}

function escapeHtml(s: string): string {
  return s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
}
```

- [ ] **Step 2: Actualizar `useTour.ts` para consumir `buildTours(t)`**

Primero leer el archivo actual:

```bash
cat src/tour/useTour.ts
```

Adaptar para:

```ts
// src/tour/useTour.ts
import { useCallback, useMemo } from "react";
// ...otros imports existentes
import { buildTours, type TourId } from "./steps";
import { useLocale } from "../i18n";

export function useTour() {
  const { t } = useLocale();
  const tours = useMemo(() => buildTours(t), [t]);

  const startTour = useCallback((id: TourId) => {
    // lógica existente, pero usando `tours[id]` en lugar del `TOURS[id]` global
  }, [tours]);

  return { startTour };
}
```

Conservar cualquier lógica de `shouldAutoStart` u otros helpers que ya tenga el archivo.

- [ ] **Step 3: Correr tests + build**

Run: `pnpm test && pnpm run build`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add src/tour/steps.ts src/tour/useTour.ts
git commit -m "refactor(i18n): migrate tour to buildTours(t) with <N> → <strong>"
```

---

### Task 31: Eliminar `utils/formatDate.ts`

**Files:**
- Delete: `src/utils/formatDate.ts`
- Modify: cualquier importador restante

- [ ] **Step 1: Verificar que no queden importers**

Run: `pnpm grep -rn 'from ".*utils/formatDate"' src/ || true`

Si aparece alguno que no fue migrado en tasks 20 o 28, migrar ahora.

- [ ] **Step 2: Borrar el archivo**

```bash
rm src/utils/formatDate.ts
```

- [ ] **Step 3: Build + tests**

Run: `pnpm test && pnpm lint && pnpm run build`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add -A src/utils/formatDate.ts
git commit -m "refactor(i18n): remove utils/formatDate, replaced by i18n/format"
```

---

### Task 32: Validación manual Fase 2

- [ ] **Step 1: Correr dev server y smoke test**

Run: `pnpm run dev`

En el browser abrir `http://localhost:5173`, verificar que:

1. Onboarding se ve igual que antes (en ES).
2. Todos los grupos cargan con tabla y partidos.
3. Llave de eliminatorias renderiza.
4. Schedule muestra fechas formateadas (esperable: "lun 11 jun · 20:00" o similar en es-AR).
5. Tour funciona en las 5 variantes (overview, groups, knockout, rooms, simulator).
6. Modales de Account y Donate abren correctamente.
7. Simulator arranca y muestra ranking.

Cualquier texto que quedó en ES literal hardcoded es un bug — hacer grep:

```bash
grep -rn 'Calendario\|Grupos\|Eliminatorias\|Salas\|Predicciones\|Resultados' src/components src/tour
```

Sólo deben aparecer dentro de strings de keys (`"topbar.mode.predictions"`, comments, etc.), no como literales en JSX.

- [ ] **Step 2: Si hubo correcciones, commit**

```bash
git add -u
git commit -m "fix(i18n): sweep stragglers in components (Phase 2 QA)"
```

---

**Fin de Fase 2.** Toda la app pasa por `t()`. EN/PT siguen siendo aliases de ES, así que la app se ve en español sin importar qué devuelva `detectLocale()`.

---

## Fase 3 — Data model de equipos

### Task 33: Quitar `name` del tipo `Team` + definir `TeamId` literal

**Files:**
- Modify: `src/types.ts`
- Modify: `src/data/teams.ts`

Este task tiene dos cambios entrelazados:
1. Borra `name` del tipo y de los datos.
2. Narrowea `Team.id` de `string` a un union literal `TeamId`. Esto hace que `t(\`teams.${team.id}\`)` sea type-safe sin casts — el template literal se expande al union `"teams.ARG" | "teams.BRA" | ...` que ya está en `MessageKey`.

- [ ] **Step 1: Export de `TEAM_IDS` literal en `data/teams.ts`**

```ts
// src/data/teams.ts
import type { Team } from "../types";

export const TEAM_IDS = [
  "MEX", "RSA", "KOR", "CZE",
  "CAN", "SUI", "QAT", "BIH",
  "BRA", "MAR", "HAI", "SCO",
  "USA", "PAR", "AUS", "TUR",
  "GER", "CUW", "CIV", "ECU",
  "NED", "JPN", "TUN", "SWE",
  "BEL", "EGY", "IRN", "NZL",
  "ESP", "CPV", "KSA", "URU",
  "FRA", "SEN", "NOR", "IRQ",
  "ARG", "ALG", "AUT", "JOR",
  "POR", "UZB", "COL", "COD",
  "ENG", "CRO", "GHA", "PAN",
] as const;

export const TEAMS: readonly Team[] = [
  // Group A
  { id: "MEX", flag: "🇲🇽", group: "A" },
  { id: "RSA", flag: "🇿🇦", group: "A" },
  { id: "KOR", flag: "🇰🇷", group: "A" },
  { id: "CZE", flag: "🇨🇿", group: "A" },
  // Group B
  { id: "CAN", flag: "🇨🇦", group: "B" },
  { id: "SUI", flag: "🇨🇭", group: "B" },
  { id: "QAT", flag: "🇶🇦", group: "B" },
  { id: "BIH", flag: "🇧🇦", group: "B" },
  // Group C
  { id: "BRA", flag: "🇧🇷", group: "C" },
  { id: "MAR", flag: "🇲🇦", group: "C" },
  { id: "HAI", flag: "🇭🇹", group: "C" },
  { id: "SCO", flag: "🏴󠁧󠁢󠁳󠁣󠁴󠁿", group: "C" },
  // Group D
  { id: "USA", flag: "🇺🇸", group: "D" },
  { id: "PAR", flag: "🇵🇾", group: "D" },
  { id: "AUS", flag: "🇦🇺", group: "D" },
  { id: "TUR", flag: "🇹🇷", group: "D" },
  // Group E
  { id: "GER", flag: "🇩🇪", group: "E" },
  { id: "CUW", flag: "🇨🇼", group: "E" },
  { id: "CIV", flag: "🇨🇮", group: "E" },
  { id: "ECU", flag: "🇪🇨", group: "E" },
  // Group F
  { id: "NED", flag: "🇳🇱", group: "F" },
  { id: "JPN", flag: "🇯🇵", group: "F" },
  { id: "TUN", flag: "🇹🇳", group: "F" },
  { id: "SWE", flag: "🇸🇪", group: "F" },
  // Group G
  { id: "BEL", flag: "🇧🇪", group: "G" },
  { id: "EGY", flag: "🇪🇬", group: "G" },
  { id: "IRN", flag: "🇮🇷", group: "G" },
  { id: "NZL", flag: "🇳🇿", group: "G" },
  // Group H
  { id: "ESP", flag: "🇪🇸", group: "H" },
  { id: "CPV", flag: "🇨🇻", group: "H" },
  { id: "KSA", flag: "🇸🇦", group: "H" },
  { id: "URU", flag: "🇺🇾", group: "H" },
  // Group I
  { id: "FRA", flag: "🇫🇷", group: "I" },
  { id: "SEN", flag: "🇸🇳", group: "I" },
  { id: "NOR", flag: "🇳🇴", group: "I" },
  { id: "IRQ", flag: "🇮🇶", group: "I" },
  // Group J
  { id: "ARG", flag: "🇦🇷", group: "J" },
  { id: "ALG", flag: "🇩🇿", group: "J" },
  { id: "AUT", flag: "🇦🇹", group: "J" },
  { id: "JOR", flag: "🇯🇴", group: "J" },
  // Group K
  { id: "POR", flag: "🇵🇹", group: "K" },
  { id: "UZB", flag: "🇺🇿", group: "K" },
  { id: "COL", flag: "🇨🇴", group: "K" },
  { id: "COD", flag: "🇨🇩", group: "K" },
  // Group L
  { id: "ENG", flag: "🏴󠁧󠁢󠁥󠁮󠁧󠁿", group: "L" },
  { id: "CRO", flag: "🇭🇷", group: "L" },
  { id: "GHA", flag: "🇬🇭", group: "L" },
  { id: "PAN", flag: "🇵🇦", group: "L" },
];

export function getTeam(id: string): Team | undefined {
  return TEAMS.find((t) => t.id === id);
}

export const GROUPS = ["A","B","C","D","E","F","G","H","I","J","K","L"] as const;
```

- [ ] **Step 2: Update `Team` type — `name` out, `id: TeamId`**

```ts
// src/types.ts
import { TEAM_IDS } from "./data/teams";

export type TeamId = (typeof TEAM_IDS)[number];

export interface Team {
  id: TeamId;       // FIFA 3-letter code, union literal
  flag: string;     // "🇦🇷"
  group: string;    // "A" through "L"
}
```

Nota: hay una dependencia circular conceptual (types.ts importa de data/teams.ts, que importa Team de types.ts). TS lo resuelve sin problema porque son imports de tipos en una dirección y valores en la otra. Si algún build complain, mover `TEAM_IDS` a `src/data/teamIds.ts` y tener tanto `data/teams.ts` como `types.ts` importando de allí.

- [ ] **Step 3: Correr TS para enumerar call sites**

Run: `pnpm tsc -b`

Expected: errores en todos los archivos que leen `team.name`. La lista aproximada es:
- `src/components/GroupView.tsx`
- `src/components/BracketView.tsx`
- `src/components/ScheduleView.tsx`
- `src/components/SimulatorView.tsx`
- Cualquier otro que el compilador identifique.

**No hacer commit todavía** — la migración se completa en las siguientes tasks.

---

### Task 34: Arreglar call sites en `GroupView`

**Files:**
- Modify: `src/components/GroupView.tsx`

- [ ] **Step 1: Reemplazar `team.name`**

Patrón: cada `team?.name` o `homeTeam?.name` se reemplaza por:

```tsx
{t(`teams.${team.id}`)}
```

Si el team viene como `homeTeam` posiblemente `undefined`, usar:

```tsx
{homeTeam ? t(`teams.${homeTeam.id}`) : "—"}
```

Como `team.id: TeamId` (union literal definido en Task 33), el template literal `` `teams.${team.id}` `` se tipa como `"teams.MEX" | "teams.RSA" | ...`, todos miembros válidos de `MessageKey`. Sin casts.

- [ ] **Step 2: Verificar compile**

Run: `pnpm tsc -b`
Expected: si quedan errores en este archivo, arreglar; si el error se fue, OK.

- [ ] **Step 3: Commit (junto con las demás en Task 37)**

Deja los cambios staged; hacemos un commit único al terminar el sweep.

---

### Task 35: Arreglar call sites en `BracketView` y `ScheduleView`

**Files:**
- Modify: `src/components/BracketView.tsx`
- Modify: `src/components/ScheduleView.tsx`

- [ ] **Step 1: Mismo patrón `t(\`teams.${id}\`)`**

En `BracketView`, los dos spots son `homeTeam.name` y `awayTeam.name` (ambos bajo check de `homeTeam ? ... : slotLabel(...)`).

En `ScheduleView`, idem.

- [ ] **Step 2: Verificar compile**

Run: `pnpm tsc -b`
Expected: errores de estos dos archivos desaparecen.

---

### Task 36: Arreglar call sites en `SimulatorView`

**Files:**
- Modify: `src/components/SimulatorView.tsx`

- [ ] **Step 1: Reemplazar las 4 apariciones de `.name`**

Hay referencias directas (`prevHomeTeam?.name`, `prevAwayTeam?.name`, `homeTeam?.name`, `awayTeam?.name`) y una que se pasaba a `getWinnerText` como string.

Dado que `getWinnerText` ya se inlineó en Task 28 para usar `t()`, ahora le pasás el nombre resuelto:

```tsx
const homeName = prevHomeTeam ? t(`teams.${prevHomeTeam.id}`) : "";
const awayName = prevAwayTeam ? t(`teams.${prevAwayTeam.id}`) : "";
const winnerText = buildWinnerText(lastResult.score, homeName, awayName, t);
```

- [ ] **Step 2: Verificar compile**

Run: `pnpm tsc -b`
Expected: 0 errores.

---

### Task 37: Arreglar `ScoreInput` y commit global de Fase 3

**Files:**
- Modify: `src/components/ScoreInput.tsx` (si pasa nombres a aria)

- [ ] **Step 1: Si `ScoreInput` recibía `team: Team` y leía `team.name`**

Reemplazar por `t(\`teams.${team.id}\`)` dentro del componente, o aceptar el nombre ya resuelto como prop — decisión del implementador según cómo esté escrito. El approach más simple: pasar `teamName: string` desde el parent.

- [ ] **Step 2: Correr todo**

Run: `pnpm test && pnpm lint && pnpm run build`
Expected: PASS. Los tests de `teams.consistency` cubren que `teams.ARG`, etc. existen.

- [ ] **Step 3: Commit único de Fase 3**

```bash
git add -A src/types.ts src/data/teams.ts src/components/GroupView.tsx \
  src/components/BracketView.tsx src/components/ScheduleView.tsx \
  src/components/SimulatorView.tsx src/components/ScoreInput.tsx
git commit -m "refactor(i18n): remove Team.name, resolve names via t(\`teams.\${id}\`)"
```

---

**Fin de Fase 3.** El modelo de datos ya es agnóstico de idioma. La app sigue en ES porque EN/PT son copias de ES, pero cualquier consumer de nombres pasa obligatoriamente por `t()`.

---

## Fase 4 — Traducciones reales EN y PT

### Task 38: Bundle EN real

**Files:**
- Modify: `src/i18n/locales/en.ts`

- [ ] **Step 1: Reemplazar el alias por un literal inline con `satisfies Messages`**

```ts
// src/i18n/locales/en.ts
import type { Messages } from "./es";

export const en = {
  common: {
    save: "Save",
    cancel: "Cancel",
    close: "Close",
    copy: "Copy",
    copied: "Copied",
    back: "Back",
    confirm: "Confirm",
    error: "Error",
    loading: "Loading...",
    retry: "Retry",
  },

  meta: {
    title: "World Cup 2026 — Prode",
    description: "Interactive World Cup 2026 fixture with predictions and P2P sync via Nostr.",
  },

  brand: { short: "World Cup 2026" },

  sidebar: {
    nav: {
      schedule: "Schedule",
      groups: "Groups",
      knockout: "Knockout",
      ranking: "Ranking",
      rooms: "Rooms",
      simulation: "Simulation",
    },
    footer: {
      myAccount: "My account",
      donate: "Donate",
      github: "GitHub",
      license: "GPL-3.0",
      copy: "© 2026 Pampeano Dev",
      githubAria: "GitHub repository",
    },
  },

  topbar: {
    namePlaceholder: "Your name",
    mode: { results: "Results", predictions: "Predictions" },
    menu: {
      sectionPredictions: "Predictions",
      sectionSimulation: "Simulation",
      sectionResults: "Results",
      sectionFixture: "Fixture",
      randomize: "Regenerate random",
      startSimulation: "Start simulation",
      exitSimulation: "Exit simulation",
      fetchApi: "Fetch from API (coming soon)",
      fetchApiTitle: "External API integration: coming soon",
      exportAll: "Export all",
      importAll: "Import all",
    },
    importError: "Import failed. Make sure it's a valid JSON file.",
  },

  groups: {
    title: "Group {group}",
    standings: {
      pos: "Pos", team: "Team", played: "P", won: "W", drawn: "D", lost: "L",
      goalsFor: "GF", goalsAgainst: "GA", goalDifference: "GD", points: "Pts",
    },
    legend: {
      qualified: "Qualifies directly",
      thirdPlace: "May advance as best third",
    },
    matches: "Matches",
  },

  knockout: {
    rounds: { R32: "Round of 32", R16: "Round of 16", QF: "Quarter-finals", SF: "Semi-finals", "3P": "Third place", F: "Final" },
    slot: {
      groupWinner: "1st {group}",
      groupRunnerUp: "2nd {group}",
      bestThird: "3rd ({groups})",
      winnerOf: "Winner {matchId}",
      loserOf: "Loser {matchId}",
    },
  },

  schedule: {
    title: "Schedule",
    empty: "No matches to show.",
    stage: {
      group: "Group {group}",
      R32: "Round of 32", R16: "Round of 16", QF: "Quarter-finals",
      SF: "Semi-finals", "3P": "Third place", F: "Final",
    },
  },

  ranking: {
    title: "Ranking",
    empty: "No points scored yet.",
    you: "You",
    headerName: "Player",
    headerPoints: "Pts",
    pointsOne: "{count} point",
    pointsOther: "{count} points",
    rivalAdd: "Add manual rival",
    rivalRemove: "Remove",
  },

  scoreInput: {
    ariaHome: "Goals {team}",
    ariaAway: "Goals {team}",
    lockedTitle: "Locked — 1 h before kickoff",
  },

  rooms: {
    list: {
      title: "Rooms",
      empty: "You're not in any room yet.",
      createCta: "Create room",
      joinCta: "Join with code",
      joinPlaceholder: "Paste the 8-character code",
      joinButton: "Join",
      invalidCode: "Invalid code.",
    },
    create: {
      title: "Create room",
      nameLabel: "Name",
      namePlaceholder: "e.g. Football buddies",
      typeOpen: "Open",
      typeClosed: "Closed (invite-only)",
      submit: "Create",
      openHelp: "Anyone with the link can join.",
      closedHelp: "Generates single-use invites.",
    },
    detail: {
      membersTitle: "Members",
      adminBadge: "Admin",
      leaveButton: "Leave room",
      leaveConfirm: "Leave this room?",
      inviteButton: "Invite",
      lockedTitle: "Room locked",
      lockedBody: "Predictions are closed.",
    },
    invite: {
      title: "Invite to room",
      linkLabel: "Shareable link",
      codeLabel: "8-character code",
      qrLabel: "Scan the QR",
      copyLink: "Copy link",
      copyCode: "Copy code",
    },
    connection: {
      connected: "Connected",
      connecting: "Connecting...",
      disconnected: "Offline",
    },
  },

  onboarding: {
    title: "World Cup 2026",
    subtitle: "Compete with your friends predicting results.",
    namePlaceholder: "Your name",
    createButton: "Start",
    restoreCta: "I already have an account",
    restoreTextarea: "Type your 12 words separated by spaces",
    restoreButton: "Restore",
    backButton: "Back",
    invalidMnemonic: "Invalid seed phrase. Check the 12 words.",
  },

  account: {
    title: "My account",
    description: "Your identity is a 12-word seed phrase (NIP-06).",
    showSeed: "Show my seed phrase",
    warning: "Don't share this with anyone. Whoever has these words controls your identity.",
    qrTitle: "Backup QR",
    copySeed: "Copy seed",
    copied: "Copied",
  },

  donate: {
    title: "Donate with Lightning",
    intro: "If this project was useful, buy me a coffee with sats:",
    addressLabel: "Lightning address",
    copied: "Copied",
    close: "Close",
  },

  simulator: {
    title: "Simulator",
    stageLabel: {
      group: "Group {group}",
      R32: "Round of 32", R16: "Round of 16", QF: "Quarter-finals",
      SF: "Semi-finals", "3P": "Third place", F: "Final",
    },
    randomButton: "Random",
    manualButton: "Manual",
    skipButton: "Skip",
    exitButton: "Exit simulation",
    winner: "{team} wins",
    draw: "Draw",
    penaltiesWinner: "{team} wins on penalties",
    previousMatch: "Previous match",
    rankingTitle: "Live ranking",
    pointsDelta: "+{delta}",
    noMoreMatches: "No more matches to simulate.",
  },

  tour: {
    overview: {
      welcomeTitle: "Welcome to Prode 2026",
      welcomeBody: "Predict all 104 World Cup matches and earn points when you get them right. <1>3 pts</1> exact score · <2>1 pt</2> winner only · <3>0</3> wrong.",
      modeTitle: "Results vs Predictions",
      modeBody: "Toggle between entering what you think will happen (Predictions) and what actually happened (Results). Your points come from comparing both.",
      groupsTitle: "Groups",
      groupsBody: "72 group-stage matches split into 12 groups (A through L). The table recalculates itself as you predict.",
      knockoutTitle: "Knockout",
      knockoutBody: "32 matches from Round of 32 through the Final. Matchups assemble automatically from your group predictions — you don't set them by hand.",
      roomsTitle: "Rooms",
      roomsBody: "Compete with friends. Create a room, share the link, and rankings sync peer-to-peer (no backend).",
      helpTitle: "Stuck later?",
      helpBody: "Tap this button on any screen to open a tour specific to what you're looking at.",
    },
    groups: {
      tabsTitle: "Groups A–L",
      tabsBody: "Switch between the 12 groups from these tabs.",
      standingsTitle: "Standings table",
      standingsBody: "Recalculates live from your predictions. Green = direct qualification. Yellow = 3rd place, may advance as best third alongside 7 others.",
      matchesTitle: "Enter a prediction",
      matchesBody: "Type the score of each match. It saves automatically. In Results mode, you enter the real result. From the ⋯ menu you can regenerate all random predictions at once.",
    },
    knockout: {
      tabsTitle: "Knockout rounds",
      tabsBody: "Navigate between Round of 32, Round of 16, Quarter-finals, Semis, and Final from these tabs.",
      autoTitle: "Automatic matchups",
      autoBody: "Teams appearing in each matchup come from your group predictions (1st of A, 2nd of B, best thirds, etc.). As you enter a match, the winner advances automatically.",
    },
    rooms: {
      createTitle: "Create room",
      createBody: "Name it and pick a type: open (anyone with the link joins) or closed (you generate single-use invites). You get an 8-character code and a shareable link.",
      joinTitle: "Join with code",
      joinBody: "If someone gave you an 8-character code, paste it here. Opening the invite link they sent is easier — it gets you in automatically.",
      commitTitle: "Commit-reveal",
      commitBody: "Your predictions are published encrypted until 1 hour before kickoff. Nobody can see them before (not the relay, not the rivals).",
    },
    simulator: {
      modeTitle: "Simulation mode",
      modeBody: "Play the entire World Cup before it starts. For each match you can simulate randomly, enter a score manually, or skip.",
      rankingTitle: "Live ranking",
      rankingBody: "After each match you see the updated ranking with arrows (↑↓) showing who moved up or down.",
      safeTitle: "Doesn't affect your real prode",
      safeBody: "When you exit simulation, everything returns to the real state. Your predictions are untouched. Refreshing the browser also reverts to the real state.",
    },
  },

  teams: {
    MEX: "Mexico", RSA: "South Africa", KOR: "South Korea", CZE: "Czechia",
    CAN: "Canada", SUI: "Switzerland", QAT: "Qatar", BIH: "Bosnia and Herzegovina",
    BRA: "Brazil", MAR: "Morocco", HAI: "Haiti", SCO: "Scotland",
    USA: "United States", PAR: "Paraguay", AUS: "Australia", TUR: "Türkiye",
    GER: "Germany", CUW: "Curaçao", CIV: "Ivory Coast", ECU: "Ecuador",
    NED: "Netherlands", JPN: "Japan", TUN: "Tunisia", SWE: "Sweden",
    BEL: "Belgium", EGY: "Egypt", IRN: "Iran", NZL: "New Zealand",
    ESP: "Spain", CPV: "Cape Verde", KSA: "Saudi Arabia", URU: "Uruguay",
    FRA: "France", SEN: "Senegal", NOR: "Norway", IRQ: "Iraq",
    ARG: "Argentina", ALG: "Algeria", AUT: "Austria", JOR: "Jordan",
    POR: "Portugal", UZB: "Uzbekistan", COL: "Colombia", COD: "DR Congo",
    ENG: "England", CRO: "Croatia", GHA: "Ghana", PAN: "Panama",
  },
} as const satisfies Messages;
```

- [ ] **Step 2: Correr tests**

Run: `pnpm test`
Expected: PASS. En particular `locales.consistency.test.ts` y `teams.consistency.test.ts` validan estructura.

- [ ] **Step 3: Commit**

```bash
git add src/i18n/locales/en.ts
git commit -m "feat(i18n): add real English translations"
```

---

### Task 39: Bundle PT real

**Files:**
- Modify: `src/i18n/locales/pt.ts`

- [ ] **Step 1: Reemplazar el alias por literal con traducciones pt-BR**

```ts
// src/i18n/locales/pt.ts
import type { Messages } from "./es";

export const pt = {
  common: {
    save: "Salvar",
    cancel: "Cancelar",
    close: "Fechar",
    copy: "Copiar",
    copied: "Copiado",
    back: "Voltar",
    confirm: "Confirmar",
    error: "Erro",
    loading: "Carregando...",
    retry: "Tentar de novo",
  },

  meta: {
    title: "Copa do Mundo 2026 — Bolão",
    description: "Tabela interativa da Copa do Mundo 2026 com palpites e sync P2P via Nostr.",
  },

  brand: { short: "Copa 2026" },

  sidebar: {
    nav: {
      schedule: "Calendário",
      groups: "Grupos",
      knockout: "Mata-mata",
      ranking: "Ranking",
      rooms: "Salas",
      simulation: "Simulação",
    },
    footer: {
      myAccount: "Minha conta",
      donate: "Doar",
      github: "GitHub",
      license: "GPL-3.0",
      copy: "© 2026 Pampeano Dev",
      githubAria: "Repositório no GitHub",
    },
  },

  topbar: {
    namePlaceholder: "Seu nome",
    mode: { results: "Resultados", predictions: "Palpites" },
    menu: {
      sectionPredictions: "Palpites",
      sectionSimulation: "Simulação",
      sectionResults: "Resultados",
      sectionFixture: "Tabela",
      randomize: "Gerar palpites aleatórios",
      startSimulation: "Iniciar simulação",
      exitSimulation: "Sair da simulação",
      fetchApi: "Buscar da API (em breve)",
      fetchApiTitle: "Integração com API externa: em breve",
      exportAll: "Exportar tudo",
      importAll: "Importar tudo",
    },
    importError: "Falha ao importar. Verifique se é um JSON válido.",
  },

  groups: {
    title: "Grupo {group}",
    standings: {
      pos: "Pos", team: "Time", played: "J", won: "V", drawn: "E", lost: "D",
      goalsFor: "GP", goalsAgainst: "GC", goalDifference: "SG", points: "Pts",
    },
    legend: {
      qualified: "Classifica direto",
      thirdPlace: "Pode passar como melhor terceiro",
    },
    matches: "Partidas",
  },

  knockout: {
    rounds: { R32: "16-avos", R16: "Oitavas", QF: "Quartas", SF: "Semis", "3P": "3º lugar", F: "Final" },
    slot: {
      groupWinner: "1º {group}",
      groupRunnerUp: "2º {group}",
      bestThird: "3º ({groups})",
      winnerOf: "Vencedor {matchId}",
      loserOf: "Perdedor {matchId}",
    },
  },

  schedule: {
    title: "Calendário",
    empty: "Sem partidas para mostrar.",
    stage: {
      group: "Grupo {group}",
      R32: "16-avos", R16: "Oitavas", QF: "Quartas",
      SF: "Semis", "3P": "3º lugar", F: "Final",
    },
  },

  ranking: {
    title: "Ranking",
    empty: "Ainda não há pontos.",
    you: "Você",
    headerName: "Jogador",
    headerPoints: "Pts",
    pointsOne: "{count} ponto",
    pointsOther: "{count} pontos",
    rivalAdd: "Adicionar rival manual",
    rivalRemove: "Remover",
  },

  scoreInput: {
    ariaHome: "Gols {team}",
    ariaAway: "Gols {team}",
    lockedTitle: "Fechado — 1 h antes do kickoff",
  },

  rooms: {
    list: {
      title: "Salas",
      empty: "Você ainda não está em nenhuma sala.",
      createCta: "Criar sala",
      joinCta: "Entrar com código",
      joinPlaceholder: "Cole o código de 8 caracteres",
      joinButton: "Entrar",
      invalidCode: "Código inválido.",
    },
    create: {
      title: "Criar sala",
      nameLabel: "Nome",
      namePlaceholder: "Ex: Galera do futebol",
      typeOpen: "Aberta",
      typeClosed: "Fechada (por convite)",
      submit: "Criar",
      openHelp: "Qualquer um com o link entra.",
      closedHelp: "Gera convites de uso único.",
    },
    detail: {
      membersTitle: "Membros",
      adminBadge: "Admin",
      leaveButton: "Sair da sala",
      leaveConfirm: "Sair desta sala?",
      inviteButton: "Convidar",
      lockedTitle: "Sala bloqueada",
      lockedBody: "Os palpites estão fechados.",
    },
    invite: {
      title: "Convidar para a sala",
      linkLabel: "Link compartilhável",
      codeLabel: "Código de 8 caracteres",
      qrLabel: "Escaneie o QR",
      copyLink: "Copiar link",
      copyCode: "Copiar código",
    },
    connection: {
      connected: "Conectado",
      connecting: "Conectando...",
      disconnected: "Offline",
    },
  },

  onboarding: {
    title: "Copa do Mundo 2026",
    subtitle: "Compita com seus amigos prevendo resultados.",
    namePlaceholder: "Seu nome",
    createButton: "Começar",
    restoreCta: "Já tenho conta",
    restoreTextarea: "Digite suas 12 palavras separadas por espaços",
    restoreButton: "Restaurar",
    backButton: "Voltar",
    invalidMnemonic: "Seed phrase inválida. Verifique as 12 palavras.",
  },

  account: {
    title: "Minha conta",
    description: "Sua identidade é uma seed phrase de 12 palavras (NIP-06).",
    showSeed: "Ver minha seed phrase",
    warning: "Não compartilhe com ninguém. Quem tiver essas palavras controla sua identidade.",
    qrTitle: "QR de backup",
    copySeed: "Copiar seed",
    copied: "Copiado",
  },

  donate: {
    title: "Doar com Lightning",
    intro: "Se o projeto te ajudou, me paga um café com sats:",
    addressLabel: "Endereço Lightning",
    copied: "Copiado",
    close: "Fechar",
  },

  simulator: {
    title: "Simulador",
    stageLabel: {
      group: "Grupo {group}",
      R32: "16-avos", R16: "Oitavas", QF: "Quartas",
      SF: "Semis", "3P": "3º lugar", F: "Final",
    },
    randomButton: "Aleatório",
    manualButton: "Manual",
    skipButton: "Pular",
    exitButton: "Sair da simulação",
    winner: "{team} vence",
    draw: "Empate",
    penaltiesWinner: "{team} vence nos pênaltis",
    previousMatch: "Partida anterior",
    rankingTitle: "Ranking ao vivo",
    pointsDelta: "+{delta}",
    noMoreMatches: "Não há mais partidas para simular.",
  },

  tour: {
    overview: {
      welcomeTitle: "Bem-vindo ao Bolão 2026",
      welcomeBody: "Preveja as 104 partidas da Copa e ganhe pontos quando acertar. <1>3 pts</1> placar exato · <2>1 pt</2> só o vencedor · <3>0</3> errou.",
      modeTitle: "Resultados vs Palpites",
      modeBody: "Alterne entre registrar o que você acha que vai acontecer (Palpites) e o que realmente aconteceu (Resultados). Seus pontos vêm da comparação.",
      groupsTitle: "Grupos",
      groupsBody: "72 partidas de fase de grupos divididas em 12 grupos (A a L). A tabela recalcula sozinha com seus palpites.",
      knockoutTitle: "Mata-mata",
      knockoutBody: "32 partidas das 16-avos até a final. Os confrontos se montam automaticamente a partir dos seus palpites de grupos — não se define na mão.",
      roomsTitle: "Salas",
      roomsBody: "Compita com amigos. Crie uma sala, compartilhe o link, e os rankings sincronizam peer-to-peer (sem backend).",
      helpTitle: "Dúvida depois?",
      helpBody: "Toque neste botão em qualquer tela para abrir um tour específico do que você está vendo.",
    },
    groups: {
      tabsTitle: "Grupos A–L",
      tabsBody: "Alterne entre os 12 grupos por estas abas.",
      standingsTitle: "Tabela de classificação",
      standingsBody: "Recalcula em tempo real com seus palpites. Verde = classifica direto. Amarelo = 3º, pode passar como melhor terceiro junto com outros 7.",
      matchesTitle: "Registrar palpite",
      matchesBody: "Digite o placar de cada partida. Salva automaticamente. No modo Resultados, você registra o placar real. Pelo menu ⋯ dá para gerar todos os palpites aleatoriamente.",
    },
    knockout: {
      tabsTitle: "Fases do mata-mata",
      tabsBody: "Navegue entre 16-avos, Oitavas, Quartas, Semis e Final por estas abas.",
      autoTitle: "Confrontos automáticos",
      autoBody: "Os times em cada confronto vêm dos seus palpites de grupos (1º de A, 2º de B, melhores terceiros, etc.). Quando você registra uma partida, o vencedor avança sozinho.",
    },
    rooms: {
      createTitle: "Criar sala",
      createBody: "Dê um nome e escolha o tipo: aberta (qualquer um com o link entra) ou fechada (você gera convites de uso único). Você recebe um código de 8 caracteres e um link.",
      joinTitle: "Entrar com código",
      joinBody: "Se alguém te passou um código de 8 caracteres, cole aqui. Abrir o link de convite é mais fácil — entra direto.",
      commitTitle: "Commit-reveal",
      commitBody: "Seus palpites são publicados criptografados até 1 hora antes do kickoff. Ninguém vê antes (nem o relay nem os rivais).",
    },
    simulator: {
      modeTitle: "Modo simulação",
      modeBody: "Jogue a Copa inteira antes dela começar. Para cada partida você pode simular aleatório, registrar um placar manual, ou pular.",
      rankingTitle: "Ranking ao vivo",
      rankingBody: "Depois de cada partida, você vê o ranking atualizado com setas (↑↓) mostrando quem subiu ou desceu.",
      safeTitle: "Não afeta seu bolão real",
      safeBody: "Ao sair da simulação, tudo volta ao estado real. Seus palpites ficam intactos. Recarregar o navegador também volta ao real.",
    },
  },

  teams: {
    MEX: "México", RSA: "África do Sul", KOR: "Coreia do Sul", CZE: "Tchéquia",
    CAN: "Canadá", SUI: "Suíça", QAT: "Catar", BIH: "Bósnia e Herzegovina",
    BRA: "Brasil", MAR: "Marrocos", HAI: "Haiti", SCO: "Escócia",
    USA: "Estados Unidos", PAR: "Paraguai", AUS: "Austrália", TUR: "Turquia",
    GER: "Alemanha", CUW: "Curaçao", CIV: "Costa do Marfim", ECU: "Equador",
    NED: "Países Baixos", JPN: "Japão", TUN: "Tunísia", SWE: "Suécia",
    BEL: "Bélgica", EGY: "Egito", IRN: "Irã", NZL: "Nova Zelândia",
    ESP: "Espanha", CPV: "Cabo Verde", KSA: "Arábia Saudita", URU: "Uruguai",
    FRA: "França", SEN: "Senegal", NOR: "Noruega", IRQ: "Iraque",
    ARG: "Argentina", ALG: "Argélia", AUT: "Áustria", JOR: "Jordânia",
    POR: "Portugal", UZB: "Uzbequistão", COL: "Colômbia", COD: "RD Congo",
    ENG: "Inglaterra", CRO: "Croácia", GHA: "Gana", PAN: "Panamá",
  },
} as const satisfies Messages;
```

- [ ] **Step 2: Correr tests**

Run: `pnpm test`
Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add src/i18n/locales/pt.ts
git commit -m "feat(i18n): add real Portuguese translations"
```

---

### Task 40: Componente `LocaleSwitcher`

**Files:**
- Create: `src/components/LocaleSwitcher.tsx`
- Create: `src/components/LocaleSwitcher.css`

- [ ] **Step 1: Crear el componente**

```tsx
// src/components/LocaleSwitcher.tsx
import { useEffect, useRef, useState } from "react";
import { NATIVE_NAMES, SUPPORTED_LOCALES, useLocale } from "../i18n";
import "./LocaleSwitcher.css";

export function LocaleSwitcher() {
  const { locale, setLocale } = useLocale();
  const [open, setOpen] = useState(false);
  const ref = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (!open) return;
    function handler(e: MouseEvent) {
      if (ref.current && !ref.current.contains(e.target as Node)) setOpen(false);
    }
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, [open]);

  return (
    <div className="locale-switcher" ref={ref}>
      <button
        type="button"
        className="locale-switcher-toggle"
        onClick={() => setOpen((v) => !v)}
        aria-label="Language"
      >
        {locale.toUpperCase()}
      </button>
      {open && (
        <div className="locale-switcher-menu" role="menu">
          {SUPPORTED_LOCALES.map((code) => (
            <button
              key={code}
              type="button"
              role="menuitem"
              className={`locale-switcher-item ${code === locale ? "active" : ""}`}
              onClick={() => {
                setLocale(code);
                setOpen(false);
              }}
            >
              <span className="locale-switcher-code">{code.toUpperCase()}</span>
              <span className="locale-switcher-name">{NATIVE_NAMES[code]}</span>
            </button>
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Crear el CSS**

```css
/* src/components/LocaleSwitcher.css */
.locale-switcher {
  position: relative;
  display: inline-block;
}

.locale-switcher-toggle {
  background: transparent;
  border: 1px solid rgba(255, 255, 255, 0.15);
  color: inherit;
  padding: 4px 10px;
  font-size: 12px;
  font-weight: 600;
  letter-spacing: 0.5px;
  border-radius: 4px;
  cursor: pointer;
}

.locale-switcher-toggle:hover {
  background: rgba(255, 255, 255, 0.08);
}

.locale-switcher-menu {
  position: absolute;
  bottom: calc(100% + 4px);
  right: 0;
  background: #1a1a1a;
  border: 1px solid rgba(255, 255, 255, 0.12);
  border-radius: 6px;
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
  min-width: 140px;
  z-index: 100;
  overflow: hidden;
}

.locale-switcher-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  width: 100%;
  padding: 8px 12px;
  background: transparent;
  border: none;
  color: inherit;
  cursor: pointer;
  font-size: 13px;
}

.locale-switcher-item:hover {
  background: rgba(255, 255, 255, 0.06);
}

.locale-switcher-item.active {
  background: rgba(255, 255, 255, 0.08);
  font-weight: 600;
}

.locale-switcher-code {
  font-size: 11px;
  opacity: 0.6;
  font-weight: 600;
  letter-spacing: 0.5px;
}

.locale-switcher-name {
  font-size: 13px;
}
```

- [ ] **Step 3: Montarlo en el `SidebarFooter`**

Modificar `src/components/SidebarFooter.tsx` para insertar `<LocaleSwitcher />` encima del row de GitHub/License (después del botón Donar):

```tsx
// dentro del <div className="sidebar-footer"> después del botón Donar
<div className="sidebar-footer-locale">
  <LocaleSwitcher />
</div>
```

Agregar el import: `import { LocaleSwitcher } from "./LocaleSwitcher";`

Agregar al CSS `SidebarFooter.css` si hace falta un wrapper:

```css
.sidebar-footer-locale {
  display: flex;
  justify-content: flex-end;
  padding: 4px 0 8px;
}
```

- [ ] **Step 4: Validar manual en browser**

Run: `pnpm run dev`

- Abrir la app, cambiar a EN: toda la UI debería renderizar en inglés.
- Cambiar a PT: toda la UI en portugués.
- Refrescar: la selección persiste.
- Los nombres de equipos cambian junto con la UI.
- Los tours en las 5 secciones aparecen en el idioma activo.

- [ ] **Step 5: Commit**

```bash
git add src/components/LocaleSwitcher.tsx src/components/LocaleSwitcher.css src/components/SidebarFooter.tsx src/components/SidebarFooter.css
git commit -m "feat(i18n): add LocaleSwitcher in SidebarFooter"
```

---

**Fin de Fase 4.** La app es trilingüe. Falta sincronizar metadatos (`<html lang>` ya se hace desde Fase 1; falta título + meta description).

---

## Fase 5 — Metadatos

### Task 41: Sincronizar `document.title` y `<meta name="description">`

**Files:**
- Modify: `src/i18n/LocaleContext.tsx`
- Modify: `index.html`

- [ ] **Step 1: Expandir el `useEffect` existente**

Reemplazar el `useEffect` actual (que solo toca `document.documentElement.lang`) por uno que también setea title y description. Depende de `value` (que incluye `t`):

```tsx
// dentro de LocaleProvider, reemplazar el useEffect actual:
useEffect(() => {
  document.documentElement.lang = locale;
  document.title = value.t("meta.title");
  const metaDesc = document.querySelector('meta[name="description"]');
  if (metaDesc) {
    metaDesc.setAttribute("content", value.t("meta.description"));
  }
}, [locale, value]);
```

- [ ] **Step 2: Agregar `<meta name="description">` a `index.html`**

```html
<!-- index.html -->
<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#1b5e20" />
    <meta name="description" content="Fixture interactivo del Mundial 2026 con predicciones y sync P2P vía Nostr." />
    <title>Mundial 2026 — Prode</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

Nota: el atributo `lang` de `<html>` arranca en `"es"` porque la default es ES; el Provider lo reemplaza en el primer render si el locale detectado es otro.

- [ ] **Step 3: Validar en DevTools**

Run: `pnpm run dev`

1. Abrir DevTools → Elements, verificar `<html lang="es">`.
2. Cambiar a EN con el switcher — `<html lang>` pasa a `"en"`, `<title>` a "World Cup 2026 — Prode", `<meta name="description">` se actualiza.
3. Repetir con PT.

- [ ] **Step 4: Commit**

```bash
git add src/i18n/LocaleContext.tsx index.html
git commit -m "feat(i18n): sync <html lang>, document.title and meta description"
```

---

### Task 42: Smoke test final

- [ ] **Step 1: Full build + test run**

Run:
```bash
pnpm test
pnpm lint
pnpm run build
pnpm run preview
```

Expected: todo verde, preview sirve la app buildeada.

- [ ] **Step 2: QA manual de los 3 locales**

En preview, con `localStorage` limpio:

1. Cambiar el idioma del browser a `es-AR`, `en-US`, y `pt-BR` sucesivamente, abrir la app con `localStorage` vacío — debería arrancar en cada uno.
2. Cambiar al switcher y volver a cargar — persiste.
3. Verificar cada ruta:
   - Sidebar, TopBar, dropdowns
   - Grupos: tabla completa con nombres de equipos traducidos
   - Knockout: rondas y slots traducidos
   - Schedule: fechas con formato correcto por locale (12h en EN, 24h en ES/PT)
   - Ranking: pluralización `1 point` vs `5 points`
   - Rooms: crear/unirse/invitar
   - Onboarding (borrar identity en localStorage para volver a verlo)
   - Simulator: labels y winner text
   - Tour (overview + 4 contextuales)
   - Account y Donate modals
   - `<html lang>` y `<title>` en DevTools

- [ ] **Step 3: Grep final anti-stragglers**

Run:
```bash
grep -rn '"[A-ZÁÉÍÓÚÑ][a-záéíóúñ ]\+"' src/components src/tour src/i18n/LocaleContext.tsx | grep -v 'locales/\|test\|__tests__\|\.css'
```

Cualquier match que no sea:
- Una clave como `"topbar.mode.predictions"`
- Un BCP47 code
- Un selector CSS
- Un id de test

...es un string hardcoded que se me pasó. Reemplazar por `t(...)` y agregar la key al bundle.

- [ ] **Step 4: Commit cualquier corrección**

```bash
git add -u
git commit -m "fix(i18n): final sweep of hardcoded strings"
```

- [ ] **Step 5: Celebrar**

`git log --oneline` debería mostrar la secuencia completa de Fase 1 → Fase 5 como commits atómicos y reversibles.

---

**Plan complete.**
